# 开发记录 2026-08-26

> 本日主题：**关闭虎牙 SDK 内部自带的崩溃上报 / 运营统计 / APM 上报（反编译 + 直接调用，零反射）**

---

## 一、会话目标与历程

| 阶段 | 时间 | 内容 |
|------|------|------|
| 需求提出 | 上午 | 去除 SDK 内部自带的崩溃上报（Bugly）和运营统计（hiido / 直播统计 / APM） |
| 方案一 | 上午 | `app/build.gradle` 直接 exclude 掉 `statistics` / `livestatistics` / `common-base-apm-sdk` 三个 aar |
| 方案一失败 | 上午 | **SDK 失效**（虎牙房间全部解析失败，抛 `NoClassDefFoundError`） |
| 方案二 | 上午 | 恢复打包三个 aar，改用**直接调用**关闭上报；用户提供反编译文档 `D:\ASDF\反编译工具\huya_sdk\文档` |
| 实施 | 下午 14:0x | `HuyaCacheGovernor` 反射 → 直接调用；新增 `NoOpReportApi`；`HuyaSDKParser` 加关闭调用 |
| 电视实测 | 14:48 | SDK 初始化正常 + 三处直接关闭调用全部生效，预解析 30 房间成功 |
| 重新编译 | 14:54-14:55 | release（强制重编）+ debug 均 BUILD SUCCESSFUL |

---

## 二、本次修改内容

### 2.1 方案一失败与根因（exclude aar → SDK 失效）

`HuyaBerryImpl.init()` 内部 `initBaseApi()` 是**无条件调用链**（无任何开关）：

```
initBaseApi()
├── HuyaStatisAgent.getInstance().getHuyaStatisApi().setGameId(...)   ← livestatistics 库
├── new ReportApiImpl() + reportApiImpl.init(appKey_hiido, ...)        ← statistics 库（hiido 通道）
├── BaseApi.init(...)
└── BaseApi.setReportApi(reportApiImpl)
Report.event("PV/init")                                                 ← statistics 库
```

把 `statistics` / `livestatistics` / `common-base-apm` 从打包中排除后，以上类的 class 缺失，SDK init 中途抛 `NoClassDefFoundError`，导致整个虎牙 SDK 初始化失败（表现：所有虎牙房间源失效）。

**结论：这三个 aar 是 SDK init 的硬依赖，不能排除。** 最终 `app/build.gradle` 只保留 `crashreport-3.0.0/4.1.9.x` + `nativecrashreport-3.7.1` 的排除（SDK 自带旧版 Bugly 与项目 Maven 版 Bugly 重复类冲突，这是既有决定）。

### 2.2 反编译确认的 SDK 开关真相

| 用户设想 | Builder 实际 | 反编译证据 |
|----------|-------------|-----------|
| `isOpenStat` | ❌ 不存在 | `HuyaBerryConfig.Builder` 仅 12 个方法 |
| `isOpenAnalytics` | ❌ 不存在 | 同上 |
| `isOpenApm` | ❌ 不存在 | 同上 |
| `isOpenReport` | ❌ 不存在 | 同上 |
| `isOpenMonitor` | ❌ 不存在 | 同上 |
| `isOpenBugly` | ✅ 存在 | `isOpenBugly(boolean)` |
| `debugMode` / `isNeedPlay` / `cameraMode` / `oneKeyGangUp` / `hidePauseBtn` / `landscapeMode` | ✅ 存在 | 共 12 个 setter |

`HuyaBerryImpl.init()` 第 63-69 行：`isOpenBugly(false)` → 跳过 `CrashHandler.init()` + `ICrashService.init()`（SDK 自带 Bugly 崩溃上报初始化）。

运营统计 / APM **没有任何 Builder 开关**，只能在 SDK init 完成后用 API 直接关闭。

### 2.3 最终方案：三处直接调用（零反射）

**① `HuyaCacheGovernor.applyOnBuilder(HuyaBerryConfig.Builder builder, Context ctx)`**
- 参数由 `Object` 改为强类型 `HuyaBerryConfig.Builder`
- 直接调用 7 个真实开关：`isOpenBugly(false)`、`debugMode(false)`、`isNeedPlay(false)`、`cameraMode(false)`、`oneKeyGangUp(false)`、`hidePauseBtn(false)`、`landscapeMode(false)`
- 删除 44 项无效反射（`isOpenStat/isOpenAnalytics/isOpenApm/...` 在 Builder 上从未命中）+ 删除 `trySetBool` 方法
- 保留目录重定向反射（cache/file → `getCacheDir()/huya_sdk`）

**② `HuyaSDKParser.init()` — SDK init 成功后追加关闭块**
```java
com.huya.ciku.apm.MonitorCenter.getInstance().stopReport();   // APM 性能/质量上报停止
com.huya.live.common.api.BaseApi.setReportApi(new NoOpReportApi()); // 统计通道替换为空实现
```
- `MonitorCenter.stopReport()` 是 APM 官方关闭入口（public static getInstance）
- `BaseApi.setReportApi(...)` 是 public static，SDK init 内部无条件注册了 `ReportApiImpl`，我们在 init 成功后**再次调用直接替换为空实现**，吞掉后续所有 PV/事件/直播统计上报

**③ `NoOpReportApi.java`（新建，`com.tv.live.util`）**
- 实现 `com.huya.live.common.api.report.ReportApi` 接口（common-base-api-sdk-14.aar），19 个方法全部空实现
- 覆盖：`init / event×6 / eventHuya / startLive / stopLive / loginSuccess / reportGuid / changeHuyaSessionId / value×2 / error / resume / pause`

### 2.4 电视实测验证（14:48，armv7 release 安装到 192.168.1.13）

```
✅ SDK 精简开关已直接调用（isOpenBugly/debugMode/isNeedPlay/cameraMode/oneKeyGangUp/hidePauseBtn/landscapeMode = false）
✅ HuyaBerry SDK 初始化成功 (init 无异常)
✅ 已直接关闭 SDK 上报: APM stopReport + 统计通道替换为空实现
🚀 预解析 30 个虎牙房间全部成功，无 NoClassDefFoundError / 无崩溃
```

---

## 三、怎么修改 / 如何修改（方法沉淀）

### 3.1 反编译工具链（无 jadx 时如何读 aar 字节码）

1. **模块定位**：在 `app/libs/*.aar` 中按 class 路径搜索，定位目标类所在库
2. **取字节码**：`aar → classes.jar → 解出目标 .class`（脚本一次性读入内存避免 zip 生命周期问题）
3. **解析常量池**：手写 Python 解析 class 文件常量池（tag 1=utf8、7=class、10=methodref…）+ 方法表（`access_flags` 判断 `public/static/interface`），即可得到完整方法签名列表，不需要 jadx
4. **验证语义**：方法签名只能看接口形状；真正的调用逻辑（如 `HuyaBerryImpl.init` 里 `isOpenBugly` 跳转）靠日志实测 + 阅读已知源码结构推断

### 3.2 SDK 模块 / 库对照表

| aar | 内容 | 关键类 |
|-----|------|--------|
| `statistics-1.5.310.aar` | 虎牙运营统计（hiido） | `Report`、`ReportApiImpl` |
| `livestatistics-1.5.310.aar` | 直播数据统计 | `HuyaStatisAgent`、`HuyaStatisApi` |
| `common-base-api-sdk-14.aar` | 基础 API | `BaseApi`、`com.huya.live.common.api.report.ReportApi`（接口） |
| `common-base-apm-sdk-14.aar` | APM 性能/质量上报（ciku） | `MonitorCenter` |
| `crashreport-*.aar` + `nativecrashreport-3.7.1.aar` | SDK 自带 Bugly | `CrashHandler`、`ICrashService` |

### 3.3 关闭 SDK 内部上报的三条路径总结

| 上报 | 关闭方法 | 位置 |
|------|----------|------|
| 崩溃上报（Bugly） | `builder.isOpenBugly(false)` | SDK init 前（Builder 阶段） |
| APM 性能/质量 | `MonitorCenter.getInstance().stopReport()` | SDK init 成功后 |
| 运营统计（PV/事件/直播统计） | `BaseApi.setReportApi(new NoOpReportApi())` | SDK init 成功后 |
| 目录写入 | 反射重定向 `cache/file` → `getCacheDir()/huya_sdk` | SDK init 前 |

---

## 四、改动文件清单（本次会话）

| 文件 | 改动 |
|------|------|
| `app/build.gradle` | 恢复打包 statistics/livestatistics/common-base-apm 三个 aar（SDK 硬依赖，exclude 会失效）；仅保留 crashreport/nativecrashreport 排除 |
| `util/HuyaCacheGovernor.java` | `applyOnBuilder` 参数改强类型 `HuyaBerryConfig.Builder`；44 项无效反射 → 直接调用 7 个真实开关；删除 `trySetBool` |
| `util/NoOpReportApi.java`（新） | 实现 `ReportApi` 接口的 19 个空方法（统计通道空实现） |
| `util/HuyaSDKParser.java` | SDK init 成功后追加 `MonitorCenter.stopReport()` + `BaseApi.setReportApi(new NoOpReportApi())` |

---

## 五、待办事项 / 后续关注

1. **`Report.event("PV/init")` 拦截缺口**：SDK init 内部那一次 `ReportApiImpl.init()` + `PV/init` 是无条件行为（无开关、发生在 `setReportApi` 替换之前），无法用当前方案拦截。影响仅限 1 次 PV 埋点；如后续要彻底拦截，只能走反射替换 `HuyaStatisAgent` 实例或 R8 空实现（与 proguard keep `com.huya.**` 冲突，需评估）
2. **实测确认统计不再上报**：后续在电视上实际播放虎牙房间，观察 logcat 无 hiido/统计上报请求、无 `startLive/stopLive` 统计事件
3. **SDK 升级复核**：若升级虎牙 SDK，需重新反编译核对 `HuyaBerryConfig.Builder` 的 setter 列表（12 个方法）与 `BaseApi.setReportApi` 签名是否变化
4. **NoOpReportApi 与 R8**：proguard 强 keep `com.huya.**` 保证 `ReportApi` 接口不被裁剪，`NoOpReportApi` 为项目自身类正常编译；Release 已实测通过
5. **APK 体积**：恢复打包后 release armv7 9.41MB（此前排除版 9.35MB），差异可接受

---

## 六、总结

- **虎牙 SDK 的"统计/APM"没有 Builder 开关**（`isOpenStat/isOpenAnalytics/isOpenApm/isOpenReport/isOpenMonitor` 均不存在），不能靠配置关闭
- **排除统计类 aar 会直接导致 SDK 失效**（`initBaseApi()` 无条件强依赖），绝不能走 exclude 路线
- 正确姿势 = **反编译确认真实 API**（`isOpenBugly` / `MonitorCenter.stopReport` / `BaseApi.setReportApi`）+ **SDK init 成功后替换空实现**，三处直接调用零反射，电视实测全部生效且 SDK 功能完好
- 本次"直接调用"改造替代了原先 44 项从未命中的反射清单，代码更清晰、更可靠、与官方 API 强绑定

---

## 七、重新编译产物（2026-08-26 14:55）

| 类型 | 文件 | 大小 | 构建时间 |
|------|------|------|----------|
| Release | `tv_live_release_arm64-v8a_2.0.103.apk` | 10.63 MB | 14:55:57（`--rerun-tasks` 强制重编） |
| Release | `tv_live_release_armeabi-v7a_2.0.103.apk` | 9.41 MB | 14:55:57 |
| Debug | `tv_live_debug_arm64-v8a_2.0.103.apk` | 19.04 MB | 14:54:34 |
| Debug | `tv_live_debug_armeabi-v7a_2.0.103.apk` | 17.56 MB | 14:54:34 |

命令：
```
gradlew.bat assembleRelease assembleDebug
gradlew.bat assembleRelease --rerun-tasks   # 强制全量重编 release
```
