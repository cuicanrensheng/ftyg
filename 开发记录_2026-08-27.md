# 开发记录 2026-08-27

> 会话主题：虎牙 SDK 上报深度关闭 + 首次点击竞态修复 + 去除设备自动降级
> 测试设备：雷电模拟器 (emulator-5554, arm64-v8a)
> 构建版本：tv_live_release_arm64-v8a_2.0.103.apk

---

## 一、虎牙 SDK Bugly 专业版彻底关闭（NoOpCrashService 兜底）

### 问题
应用自身已集成 Bugly 普通版做崩溃监控，虎牙 SDK 内置的 Bugly 专业版（AppID `ccd26cafc4`，含 RQD/Beta/Block 模块）完全重复，必须彻底关闭。

### 修改文件

#### 1. 新建 `app/src/main/java/com/tv/live/util/NoOpCrashService.java`
- `extends AbsService implements ICrashService`
- `init()` 空实现 → 不调用 `CrashReport.initCrashReport(context,"ccd26cafc4",...)`
- `postCatchedException(Throwable)` 空实现 → 异常不上报到 Bugly 专业版

#### 2. 修改 `app/src/main/java/com/tv/live/util/HuyaSDKParser.java`
- 在 `sHuyaBerry.init()` **之前**通过 `ServiceHelper.createService(ICrashService.class, NoOpCrashService.class)` 注册兜底服务
- ServiceCenter.addService() 不覆盖已注册的 key → SDK init 内部 `createService(ICrashService, CrashService)` 被静默忽略
- init 后二次验证：`getService(ICrashService.class)` 取回确认仍为 NoOpCrashService

#### 3. 修改 `app/proguard-rules.pro`
- 新增 `-keep class NoOpCrashService { <init>(); }` 防止 R8 删除构造器
- 补充 `-keep class NoOpReportApi { *; }`

#### 4. 修改 `app/src/main/java/com/tv/live/util/HuyaCacheGovernor.java`
- 更新关闭分工注释，补充 NoOpCrashService 兜底层

### SDK 上报关闭 4 步清单

| 步骤 | 作用 | 代码 | 状态 |
|---|---|---|---|
| Step 1 | 关闭 Bugly 崩溃上报（主） | `isOpenBugly(false)` | ✅ |
| Step 2 | 关闭 Hiido 运营统计 | `BaseApi.setReportApi(NoOpReportApi)` | ✅ |
| Step 3 | 关闭 ciku APM 性能监控 | `MonitorCenter.stopReport()` | ✅ |
| Step 4 | 防御性替换 CrashService（兜底） | `ServiceHelper.createService(ICrashService.class, NoOpCrashService.class)` | ✅ 本次新增 |

### Bugly 专业版模块关闭验证日志
```
[10:07:02] [Bugly专业版] Step1 NoOpCrashService 注册: ICrashService=com.tv.live.util.NoOpCrashService ✅ 注册成功
[10:07:02] [Bugly专业版] init后验证: ICrashService=com.tv.live.util.NoOpCrashService ✅ 仍为NoOp(SDK未覆盖)
[10:07:02] [Bugly专业版] isOpenBugly=false → CrashReport.initCrashReport(context,"ccd26cafc4",...) 未执行 ✅
[10:07:02] [Bugly专业版] 专业版模块未激活: BuglyRqdModule ✅ BuglyBetaModule ✅ CRASHTYPE_BLOCK(ANR) ✅
[10:07:02] [SDK上报验证] Step4 统计通道: ReportApi=com.tv.live.util.NoOpReportApi ✅ 已替换为NoOp
[10:07:02] [SDK上报验证] ====== 全部关闭成功 ✅✅✅ ======
```

---

## 二、首次点击虎牙频道 SDK 失效竞态修复

### 问题
用户首次点击虎牙频道时提示「虎牙 SDK 不可用，无法解析直播源」，切换频道后又恢复正常。

### 根因
竞态条件：`initHuyaSDK()` 有 **10 秒延迟** + HandlerThread 异步初始化。用户在 `sInitOk=false` 时点击 → `playHuyaStream()` 立即报错，无等待重试机制。

### 修改文件

#### 1. `app/src/main/java/com/tv/live/util/HuyaSDKParser.java`
- 新增 `waitForInit(timeoutMs)` — 阻塞等待 SDK 就绪（同步场景）
- 新增 `addInitReadyListener(Runnable)` — 注册就绪回调（异步场景，SDK ready 后自动执行）
- 新增 `sInitWaitLock` + `notifyAll()` 机制：所有 init 成功/失败路径都通知等待者
- `parseFull()` 改为：SDK 未就绪时注册回调，就绪后自动重试解析
- 所有 init 失败路径（Mars 预检失败、HuyaBerry.instance() 返回 null、绑定异常）均添加 `notifyAll()`

#### 2. `app/src/main/java/com/tv/live/TVPlayerManager.java`
- `playHuyaStream()` 不再立即报错，改为 `addInitReadyListener` 等待 SDK 就绪后自动重试
- 添加 15 秒超时保护：超时后降级为错误提示

#### 3. `app/src/main/java/com/tv/live/util/HuyaStreamPlayer.java`
- `playHuyaStream()` 同步采用等待重试策略
- 添加 15 秒超时保护

#### 4. `app/src/main/java/com/tv/live/MyApplication.java`
- SDK init 延迟从 **10s → 2s**（init 已在 HandlerThread 执行不阻塞主线程，10s 过长是竞态主因）

### 测试结果
- 模拟器全量日志分析：**0 条** "虎牙 SDK 不可用" 错误
- SDK init 耗时约 4s（2s 延迟 + ~2s 初始化）
- 预解析正确等待 SDK init：`⏳ SDK 尚未 init，已缓存 30 个房间号`
- init 成功后立即补发预解析：`🔄 SDK init 完成，补发 30 个房间`

---

## 三、去除手机/电视自动降级画质功能

### 问题
`HuyaSDKParser.selectBestStreamForDevice()` 根据 `DeviceCapabilities.isTv()` 自动降级：
- 电视 → 固定 720p
- 手机 → 自动选最高码率

用户希望所有设备统一使用最高画质，由用户手动切换清晰度。

### 修改文件

#### 1. `app/src/main/java/com/tv/live/util/HuyaSDKParser.java`
- `selectBestStreamForDevice()` 移除 `DeviceCapabilities.isTv()` 检测
- 统一 `targetHeight = Integer.MAX_VALUE`，所有设备选最高可用码率
- 原日志 `【适配选择】设备类型=电视(固定720p)` → 移除

#### 2. `app/src/main/java/com/tv/live/TVPlayerManager.java`
- 日志 `【电视适配】默认流已调整...(适配目标高度=720p)` → `【画质选择】默认流已调整...(统一最高画质)`

### 影响范围
- `DeviceCapabilities` 类仍保留（BuglyLogSender 中仍用于上报设备类型）
- `VariantManager.switchToResolution()` 不受影响（无设备检测逻辑）
- 用户可在播放界面手动切换清晰度

---

## 四、新增 SDK 上报关闭验证日志

为便于从 9527 端口拉取日志验证 SDK 上报关闭状态，添加了详细的验证日志：

### Bugly 专业版专项验证日志
```
[SDK上报验证] Step1 Bugly兜底: ICrashService=com.tv.live.util.NoOpCrashService ✅ NoOp注册成功
[Bugly专业版] init后验证: ICrashService=com.tv.live.util.NoOpCrashService ✅ 仍为NoOp(SDK未覆盖)
[SDK上报验证] Step4 统计通道: ReportApi=com.tv.live.util.NoOpReportApi ✅ 已替换为NoOp
[SDK上报验证]   Bugly崩溃(主):   isOpenBugly=false ✅ (Builder)
[SDK上报验证]   Bugly崩溃(兜底): NoOpCrashService ✅ 注册成功且未被覆盖
[SDK上报验证]   APM性能监控:     stopReport ✅
[SDK上报验证]   运营统计PV/事件: NoOpReportApi ✅
[SDK上报验证]   debugMode/cameraMode/oneKeyGangUp等=false ✅ (Builder)
[SDK上报验证] ====== 全部关闭成功 ✅✅✅ ======
```

### 日志拉取方式
```
# 从应用自身 9527 端口拉取日志
curl http://127.0.0.1:9527/api/logs

# 清空日志
curl http://127.0.0.1:9527/api/logs?clear=true

# 过滤 SDK 上报验证
curl http://127.0.0.1:9527/api/logs | jq '.logs[] | select(.message | test("SDK上报|Bugly专业版|NoOp"))'
```

---

## 五、文件变更汇总

| 文件 | 操作 | 说明 |
|---|---|---|
| `NoOpCrashService.java` | 新建 | Bugly 专业版兜底空实现 |
| `HuyaSDKParser.java` | 修改 | 等待重试机制 + Bugly 兜底注册 + 验证日志 + 去除设备降级 |
| `TVPlayerManager.java` | 修改 | 播放等待重试 + 日志更新 |
| `HuyaStreamPlayer.java` | 修改 | 播放等待重试 |
| `MyApplication.java` | 修改 | init 延迟 10s → 2s |
| `HuyaCacheGovernor.java` | 修改 | 注释更新 |
| `proguard-rules.pro` | 修改 | 新增 NoOpCrashService/NoOpReportApi keep 规则 |

---

## 六、待办事项

- [ ] 真机（创维/酷开/小米电视）回归测试 SDK 上报关闭验证
- [ ] 真机验证首次点击频道不再出现"SDK 不可用"错误
- [ ] 真机验证画质统一为最高码率
- [ ] 考虑将 SDK init 延迟进一步降至 0s（HandlerThread 不阻塞主线程）
- [ ] Android 5.1.1 低版本兼容性回归

---

## 七、UDB 设备指纹上报拦截（方案1：host 字段改写）

> 会话时间：2026-08-27（下午）
> 测试设备：雷电模拟器 (emulator-5554, arm64-v8a)
> 反编译来源：`D:\ASDF\反编译工具\huya_sdk`
>    - `huyaudbunify-1.0.80007.1.1`（UDB 统一登录）
>    - `hydeviceid-1.5.24-np`（设备指纹 SDK）

### 背景
应用已关闭虎牙 SDK 的 Bugly/Hiido 统计，但漏掉了 **UDB 设备指纹 SDK**：
- `huyaudbunify` 的 `com.huyaudbunify.HuyaAuth.init()` 首次启动调用
  `com.huya.security.DeviceFingerprintSDK.getInstance().getCDID()` → 触发指纹上报
- `DeviceFingerprintSDK`（`hydeviceid` aar）是**单例 + 无外部开关**，
  `getInstance()` 强制 `init()` → `workThread.start()`，
  后台自动向 `udbdf.huya.com` / `udbdf-v2.nimo.tv` / `api-cloud.master.live` 上报设备指纹
  （`/device/fingerprint/log`、`/check`、`/link`、`/dckey/check`）
- `HyDeviceChecker.check()` 另持独立静态字段 `urlDeviceChecker`
  （默认 `https://udbdf.huya.com/dckey`），用于设备判定/风控

### 调用链
```
MyApplication.initHuyaSDK()
  → HuyaSDKParser.init(app)
    → sHuyaBerry.init(app, config)        // HuyaBerryImpl
      → HuyaAuth.init(...)                // 内部
        → DeviceFingerprintSDK.getInstance()   // ← 触发上报线程
          → workThread → POST udbdf.huya.com /fingerprint/* + /dckey/check
```

### 方案1：改写 host 字段指向无效地址（已实施 ✅）
SDK 内部拼出的 URL 即指向 127.0.0.1，上报线程空转、零流量。

#### 修改 1. `app/src/main/java/com/tv/live/util/HuyaSDKParser.java`
在「官方精简开关」区块之后、`sHuyaBerry.init()` 执行之前，新增：
```java
final String DEAD_HOST = "http://127.0.0.1";
com.huya.security.DeviceFingerprintSDK.host = DEAD_HOST;
com.huya.security.DeviceFingerprintSDK.kiwiHost = DEAD_HOST;
com.huya.security.DeviceFingerprintSDK.nimoHost = DEAD_HOST;
com.huya.security.DeviceFingerprintSDK.openApiHost = DEAD_HOST;
// HyDeviceChecker 独立静态字段 urlDeviceChecker 一并改写
com.huya.security.hydeviceid.HyDeviceChecker.setUrlDeviceChecker(DEAD_HOST);
```
- `com.huya.security.*` 类未被混淆，可直接引用无需反射
- 验证日志：`[设备指纹] UDB host 已改写指向 127.0.0.1 → udbdf.huya.com 等上报全部失效 ✅`

#### 修改 2. `app/proguard-rules.pro`
新增 `-keep class com.huya.security.** { *; }`，确保 release 中 host 字段不被混淆/内联，拦截逻辑在 release 同样生效。

### 抓包验证结果（雷电 tcpdump）
- ✅ **UDB 设备指纹域名零流量**：`udbdf.huya.com` / `udbdf-v2.nimo.tv` / `api-cloud.master.live` 无任何 DNS 查询
- 应用自身 Bugly（`android.bugly.qq.com`）仍有请求 → **按用户要求保留不动**（属正常业务依赖）

### 尝试过的网络层拦截（已回退 ⚠️）
曾尝试额外加全局 `URLStreamHandlerFactory`（网络层拦截所有指纹域名请求），但实现有缺陷导致
`StackOverflowError` 崩溃：非黑名单 URL 调用 `URL.openConnection()` 又回到自身 → 无限递归。
**已撤销该方案**，仅保留方案1 字段改写（稳定可用）。

> 若日后需重做网络层拦截，正确做法：非黑名单 URL 必须委托
> `URL.getURLStreamHandler(protocol)`（反射获取系统默认 handler，绕过已安装的 factory），
> 而非再调 `URL.openConnection()`。

---

## 八、构建产物与遗留问题

### 本次构建
- `assembleDebug` + `assembleRelease` 均 BUILD SUCCESSFUL
- 产物：`tv_live_debug_arm64-v8a_2.0.103.apk` / `tv_live_debug_armeabi-v7a_2.0.103.apk`
- 产物：`tv_live_release_arm64-v8a_2.0.103.apk` / `tv_live_release_armeabi-v7a_2.0.103.apk`

### 遗留警告（非阻塞）
- `proguard-rules.pro:337` R8 警告：`Option -repackageclasses overrides -flattenpackagehierarchy`
  → 建议后续二选一：移除 `-flattenpackagehierarchy` 或 `-repackageclasses`，避免混淆配置冲突

### 新增待办
- [ ] 将本次 UDB 指纹拦截（方案1）在真机（电视）上抓包复验
- [ ] 评估是否需要对 `com.duowan.kiwi`（OAID）做进一步处理（当前无独立上报域名，暂不处理）
- [ ] 清理 proguard 第337行 `-repackageclasses` / `-flattenpackagehierarchy` 冲突警告
- [ ] 若后续需网络层兜底拦截，用反射系统默认 handler 方案重做（勿用 URL.openConnection 递归）

---

## 九、虎牙一起看分组（SDK 独立路线）+ 主线程阻塞彻底解决

> 会话时间：2026-08-27（晚间）
> 测试设备：雷电模拟器 (emulator-5554, arm64-v8a)
> 构建版本：tv_live_debug_arm64-v8a_2.0.103.apk

### 背景需求
1. 新增虎牙一起看固定分组（虎牙电影 / 虎牙电视剧 / 虎牙动漫 / 虎牙综艺 / 虎牙一起看）
2. **用户自己的直播源分组优先**，不被虎牙分组排到后面
3. 虎牙分组管理器与分组加载**全部子线程执行**，不得阻塞主线程
4. 虎牙分组内的弹窗（正在加载 / 加载失败 / 正在获取中 / 暂无频道）全部屏蔽
5. 编译实测启动不再阻塞主线程

---

### 1. 新建 `HuyaTogetherWatchGroupManager.java`（虎牙一起看分组管理器）

位置：`app/src/main/java/com/tv/live/manager/HuyaTogetherWatchGroupManager.java`

**定位：** 本管理器是「虎牙一起看」频道列表的**独立获取路线**，唯一来源为 SDK 内部 API：
```
① HuyaSDKParser.getTagList()            → SDK 分类 TagInfo(id,name)
② HuyaSDKParser.getLiveListByTag(tagId) → 按 tagId 拉取 LiveListInfo 频道
```
不读 HTTP 接口、不读本地兜底文件、不合并其他路线的数据 → **不受其他路线分组干扰**。

**固定 5 组**（顺序即展示顺序，虎牙一起看在最后）：
```
虎牙电影 / 虎牙电视剧 / 虎牙动漫 / 虎牙综艺 / 虎牙一起看
```

**频道量策略（SDK 范围内提升频道数）：**
| 策略 | 实现 |
|---|---|
| 单 tag 翻页 | `MAX_TAG_PAGES=10` 页（每页约 20 个，SDK isMore 翻到底自动停止） |
| 宽匹配聚合 | `GROUP_KEYWORDS` 按标题/昵称关键词把多 tag 频道归入同组 |
| 兜底收容 | 未命中细分分组的 tag 一律收容到「虎牙一起看」混合组，不丢弃 |

**频道标记：** 产出的 Channel 一律 `setHuyaSdkTogetherWatch(true)`，标识来源为 SDK 内部一起看列表（独立分组，不受其他源影响）。

**防重/健壮性：**
- 去重基准 `LinkedHashSet<Long> seenUids`（uid 优先），跨页去重
- 翻页保护：`newCount==0` 或 SDK 报错即停止，单 tag 超时 `SDK_TAG_TIMEOUT_SEC=8s`
- 过滤「精彩直播 / 精彩节目」占位条目（通常为已下播/无效频道）
- 过滤 gameId≠2135（一起看分类）的混入推荐（防王者荣耀等污染分组）
- 长整型 ID 溢出修复：只有 `<=Integer.MAX_VALUE` 才是真正短房间号，13 位长 ID 是 uid 不能强转 int
- 主播 UID 存在 → `huya://uid/<uid>` 协议（SDK getLiveData(uid) 播放成功率最高）；无 UID 但有短房间号 → `huya://room/<room>` 协议

---

### 2. 用户自己的直播源分组优先（不被虎牙分组排到后面）

修改 `app/src/main/java/com/tv/live/widget/GroupListManager.java`：
- 新增 `setFixedGroups(String[] names)`：注册固定分组名单（虎牙 5 大组）
- 新增 `isFixedGroup(int position)`：判断某位置是否为固定分组
- `setGroups()` 重构排序：
  ```java
  groupNameList.add(GROUP_ALL);              // ① 全部
  groupNameList.addAll(originalGroups);      // ② 用户自己的直播源分组（原样保留，m3u 自带分组不动）
  groupNameList.addAll(fixedGroupNames);     // ③ 虎牙一起看固定分组（排在最后）
  ```
- 虎牙固定分组频道（`isHuyaSdkTogetherWatch`）**不生成普通分组**，避免与固定分组入口重复
- 普通分组与固定分组重名时以固定分组为准

调用点：`ChannelPanelController.setChannels()` 中 `groupListManager.setFixedGroups(HuyaTogetherWatchGroupManager.GROUP_NAMES)`。

---

### 3. 虎牙分组管理器/分组加载全部子线程（不阻塞主线程）

#### 3.1 `HuyaTogetherWatchGroupManager`
- 线程池：`Executors.newFixedThreadPool(4)`（`CONCURRENCY=4`）
- `fetchAllGroups()` / `fetchGroup()` 均为**异步方法**：校验通过后 `mExecutor.execute(...)` 在子线程执行阻塞拉取，成功后回调切回主线程（`mMainHandler.post`）
- 并发拉取组内所有 tag：`mExecutor.submit(new Callable<List<Channel>>(...))` 并行执行 `fetchTagBlocking`，`Future.get` 汇总后按 roomId 去重填入 Group
- 防重入：`mFetching.compareAndSet(false, true)`，拉取中重复请求回 `postOnError(listener, "正在获取中，请稍候")`
- 每个 tag 阻塞拉取用 `CountDownLatch.await(timeout)` 等待 SDK 回调，超时不卡死

#### 3.2 `HuyaSDKParser` 预解析调度彻底移出主线程
原实现：预解析 `Handler` 绑定**主线程 Looper**，每房间约 1.5s 解析且 SDK 回调也回主线程 → 启动时持续占用 UI 线程。

现实现（`app/src/main/java/com/tv/live/util/HuyaSDKParser.java`）：
```java
private static final HandlerThread sPreloadThread;   // 独立后台线程
static {
    sPreloadThread = new HandlerThread("HuyaSDKParser-Preload");
    sPreloadThread.setDaemon(true);
    sPreloadThread.start();
    sPreloadHandler = new Handler(sPreloadThread.getLooper());
}
```
- 限速串行：每房间间隔 `PRELOAD_INTERVAL_MS=1500ms`，最多预解析 `PRELOAD_MAX_ROOMS=30` 个（省 CPU+流量）
- 预解析 listener 静默：只写日志，不弹 Toast、不阻塞 UI
- 主线程只做 `preloadRooms()` **入队追加**（毫秒级 add 操作），解析全在子线程

#### 3.3 `ChannelPanelController.loadFixedGroupChannels()`
- 缓存命中（`loadedFixedGroups.contains(groupName)`）→ 直接本地过滤，无网络
- 未命中 → 清空列表 → `HuyaTogetherWatchGroupManager.fetchGroup(groupName, ...)` 异步拉取 → 回调主线程按 `channelId/huyaRoomId` 去重追加到 `channelSourceList` → `setGroups` + `setChannels` 刷新
- 虎牙一起看频道加入后**立即触发预解析**：`HuyaSDKParser.preloadRooms(twRoomIds)`（与直播源显示并行，点频道时命中缓存 1s 内出画）

---

### 4. 虎牙分组内弹窗全部屏蔽（仅保留日志）

修改 `app/src/main/java/com/tv/live/manager/ChannelPanelController.java`，3 处 Toast 注释化（`LogBridge` 日志保留）：

| 行 | 弹窗内容 | 处理 |
|---|---|---|
| 302 | `正在加载「分组」…` | 注释 |
| 312 | `「分组」暂无频道` | 注释 |
| 359 | `「分组」加载失败: <errMsg>`（含"正在获取中，请稍候"） | 注释 |

`MainActivity` 全局「加载失败，请检查网络或稍后重试」按用户要求**保留不动**。

---

### 5. 编译实测：启动不再阻塞主线程 ✅

构建：`BUILD SUCCESSFUL` → 安装 `tv_live_debug_arm64-v8a_2.0.103.apk` → 冷启动抓全量 logcat 分析：

| 检查项 | 结果 |
|---|---|
| ANR / Choreographer 跳帧 / Slow Looper | **0 条** |
| 主线程日志时间戳长间隙（>300ms） | **0 处** |
| 预解析执行线程 | 全部在子线程 `HuyaSDKParser-Preload`（TID=7725） |
| 预解析网络回调线程 | 7635 / 7636 / 7613（子线程，非主线程 7586） |
| 主线程对预解析的参与 | 仅 `🔀追加 N 个房间到队尾`（毫秒级入队） |
| 单房间预解析耗时 | ≈0.1s（如 `56.213 入队 → 56.319 成功`，之前主线程每房间 1.5s） |
| 弹窗屏蔽 | 日志出现 `虎牙固定分组[虎牙综艺]加载失败: 正在获取中，请稍候` 时仅 `W ChannelPanel` 日志，无 Toast |

**结论：** 预解析与虎牙分组拉取已全部移出主线程，冷启动全程主线程无任何 >300ms 阻塞、无 ANR、无跳帧；虎牙分组弹窗已静默化。

---

### 6. 文件变更汇总（今晚）

| 文件 | 操作 | 说明 |
|---|---|---|
| `HuyaTogetherWatchGroupManager.java` | 新建 | 虎牙一起看 5 大分组管理器（SDK 独立路线，全子线程） |
| `GroupListManager.java` | 修改 | `setFixedGroups` / `isFixedGroup`；用户分组优先排序；固定分组去重/防重复 |
| `ChannelPanelController.java` | 修改 | 固定分组加载（缓存命中+异步拉取）；3 处弹窗注释化；预解析并行触发 |
| `HuyaSDKParser.java` | 修改 | 预解析 Handler 从主线程 Looper 迁至独立 HandlerThread |
| `Channel.java` | 修改 | 新增 `isHuyaSdkTogetherWatch` 标记 |

### 新增待办
- [ ] 真机（电视）回归虎牙 5 大固定分组加载与播放
- [ ] 观察「虎牙一起看」混合组频道量是否充足（必要时放宽关键词/翻页页数）
- [ ] 预解析间隔 1.5s 在真机低配设备上的 SDK 并发表现复核
