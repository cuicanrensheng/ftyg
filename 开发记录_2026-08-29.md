# 开发记录 2026-08-29

> 本日主题：**SDK 初始化失败根因分析（恢复 libhycrypto.so / libmmkv.so，回退错误 exclude）
> + 六轮体积优化（N: nsdt stub / O: hydeviceid 移除 / P: BoringSSL 定案 / Q: mtpencrypt 移除 / R: ELF 紧凑重写裁剪），
> 最终 release arm64 5.37MB / v7a 5.14MB**

---

## 一、问题背景

| 项 | 内容 |
|----|------|
| 现象 | 编译 release + debug 装机后，HuyaBerry.init 抛 `UnsatisfiedLinkError: dlopen failed` |
| 错误链路 | `HuyaBerry.init()` → 静态初始化链 → 某个 System.loadLibrary("...") → dlopen 失败 |
| 排查方式 | `adb reverse tcp:9527 tcp:9527` + `Invoke-RestMethod http://127.0.0.1:9527/api/logs` 拉取结构化日志 |
| 设备 | 雷电模拟器 emulator-5554 + 127.0.0.1:5555（均为 x86_64 跑 arm64-v8a APK） |

---

## 二、错误根因（两轮排查）

### 2.1 第一轮：libhycrypto.so 排除导致 dlopen 失败

| 项 | 内容 |
|----|------|
| 现象 | `dlopen failed: library "libhycrypto.so" not found: needed by libmarsstn.so` |
| 根因 | 昨日决策 excludes `**/libhycrypto.so`（1.72MB），但 `libmarsstn.so` 与 `libhyssl.so` 的 DT_NEEDED 硬依赖 hycrypto，dlopen 强制解析 |
| 决策 | 恢复保留 libhycrypto.so（不可排除） |
| Stub 评估 | libhycrypto 导出 2272 个符号（完整 BoringSSL），libhyssl 引用 326 个 SSL 符号；空实现会让 SDK SSL/TLS 通道全失效（虎牙拉流 HTTPS 失败），stub 工作量等同重移植半个 BoringSSL |

#### readelf 符号依赖分析（关键数据）

```
libhycrypto.so defined symbol count: 2272 (X509/EVP/AES/SHA/RSA/EC/HMAC/BIO/ssl_*/...)
libmarsstn.so UND total: 208 → from hycrypto: 6
  (X509_STORE_CTX_get_error, ERR_clear_error, ERR_free_strings,
   X509_VERIFY_PARAM_set_hostflags, ERR_peek_error, X509_VERIFY_PARAM_add1_host)
libhyssl.so UND total: 366 → from hycrypto: 326 (SSL_CTX_new/SSL_read/SSL_write/EVP_aead_*/...)
libmarsstn.so DT_NEEDED: libmarsxlog.so, libc++_shared.so, libhyquic.so, liblog.so,
                        libhycrypto.so, libhyssl.so, libm.so, libdl.so, libc.so
libhyssl.so DT_NEEDED: libhycrypto.so, libm.so, libstdc++.so, libdl.so, libc.so
```

### 2.2 第二轮：libmmkv.so 排除导致 HuyaBerry.init 失败

| 项 | 内容 |
|----|------|
| 现象 | `HuyaBerry init 失败: UnsatisfiedLinkError: dlopen failed: library "libmmkv.so" not found` |
| 堆栈 | `com.duowan.kiwi.barrage.MmkvConfigImpl.getSpImpl(MmkvConfigImpl.java:18)` |
| 根因 | 昨日决策 excludes `**/libmmkv.so`，但 kiwi-barrage 弹幕 SDK 在 init 后期静态初始化阶段 `System.loadLibrary("mmkv")`；本应用 isNeedPlay(false) 仍走弹幕配置路径，MMKV 必加载 |
| 决策 | 恢复保留 libmmkv.so（注意：AAR 内当前为 **stub 版** arm64 8.7KB / v7a 6KB，原版约 450KB 在 `app/libs/mmkv-1.0.12.aar.bak`；exclude 后 so 整个缺失才报 not found，与 stub 无关） |

---

## 三、修复方案

### 3.1 build.gradle 修改（app/build.gradle）

```gradle
jniLibs {
    excludes += ['**/libBugly_Native.so']
    // 2026-08-29 验证：exclude → HuyaBerry init failed，不可剔除
    // excludes += ['**/libmmkv.so']
    // 2026-08-29 验证：exclude → dlopen failed，不可剔除
    // excludes += ['**/libhycrypto.so']
    pickFirsts += [
        // 本地 stub so 优先于 aar 中的同名 SDK so
        'lib/arm64-v8a/libudbauthunify.so',
        'lib/armeabi-v7a/libudbauthunify.so',
        'lib/arm64-v8a/libhyquic.so',
        'lib/armeabi-v7a/libhyquic.so'
    ]
}
```

### 3.2 不可剔除清单（硬依赖）

| so | 大小 | 不可剔除原因 |
|----|------|------------|
| libhycrypto.so | 1.72MB | libmarsstn.so + libhyssl.so DT_NEEDED 硬依赖 |
| libmmkv.so | 原版 ~450KB / **当前 stub 版 8.7KB（arm64）、6KB（v7a）** | kiwi-barrage 弹幕 SDK System.loadLibrary 必经 |
| libmarsxlog.so | - | xlog 链路（Mars STN 异步日志） |
| libstlport_shared.so | - | C++ STL 运行时 |
| libc++_shared.so | - | libmarsstn.so DT_NEEDED 硬依赖 |

### 3.3 可剔除/可 stub 清单

| so | 处理 | 节省 |
|----|------|------|
| libhyquic.so | C++ Stub（16 个 posix_quic 接口空实现） | -4.09MB |
| libudbauthunify.so | C++ Stub（4 个 JNI 接口空实现） | -4.39MB |
| sub-gamesdk-webview-release.aar | excludes | -92KB |

---

## 四、验证结果（双模拟器 9527 端口日志）

### 4.1 emulator-5554（`_emu9527_v2.txt` 1127 条）

```
20:42:24 HuyaBerryReportGuard: ✅ sendPlayerData 播放数据上报已禁用
20:42:24 HuyaBerryReportGuard: ✅ setGameAccountID("") 已清空游戏账号绑定
20:42:24 HuyaSDKParser: ✅ HuyaBerry SDK 初始化 & 绑定完成
20:42:24 HuyaSDKParser: ✅ HuyaBerry SDK 初始化成功 (init 无异常)
20:42:24 HuyaSDKParser: [2/4] HuyaBerryConfig build 完成
```

### 4.2 127.0.0.1:5555（`_emu9528_v2.txt` 1127 条）

完全一致，零 dlopen 错误、零 UnsatisfiedLinkError、零 Exception。

### 4.3 grep 关键错误（应为空）

```bash
grep -E "dlopen|UnsatisfiedLinkError|ERROR|FAIL|FATAL" _emu9527_v2.txt
# No matches found
```

---

## 五、APK 体积对比

| 版本 | arm64-v8a | armeabi-v7a |
|------|-----------|--------------|
| 正式 release（本节时间点：恢复 mmkv/hycrypto 后） | 7.73MB | 7.13MB |
| 调试 debug | 16.05MB | 15.32MB |
| **最终 release（R 项 ELF 裁剪后，见十三）** | **5.37MB** | **5.14MB** |

> ⚠️ 上文 7.73/7.13MB 是本节时间点的中间态快照；截至本日收尾，最终 release 已到
> **5.37/5.14MB**（链路：7.73 → 7.09（N 项）→ 6.72（O 项）→ 5.37（R 项））。
> 相比未优化前（>18MB）已缩减 70%+，主要来自 libhyquic / libudbauthunify stub +
> 各 aar 排除 + ELF 紧凑重写（见十三）。

---

## 六、文件变更

| 文件 | 操作 | 原因 |
|------|------|------|
| `app/build.gradle` | 修改 | 注释掉 libmmkv.so / libhycrypto.so 的 excludes（已恢复保留），保留决策结论与验证日志 |
| `app/src/main/cpp/nsdt_stub.cpp` | 新增 | libnsdt.so 的 8 个 JNI 方法空实现 stub（见八） |
| `app/src/main/cpp/CMakeLists.txt` | 修改 | 添加 nsdt stub 编译目标 |
| `app/src/main/java/com/huya/security/*` | 新增 | hydeviceid 移除后的本地 stub 类（见九） |
| `app/src/main/java/com/huya/mtp/encrypt/*` | 新增 | mtpencrypt 移除后的纯 Java 替代（见十二） |
| `_elf_shrink.py` | 新增 | ELF 紧凑重写裁剪脚本（见十三） |
| `app/src/main/jniLibs/{arm64-v8a,armeabi-v7a}/lib{hycrypto,hyssl}.so` | 修改 | 真实 BoringSSL + ELF 裁剪版（见十一/十三） |
| `app/libs/hyssl-0.1.11.aar` | 修改 | jni/ 内真实 BoringSSL 提取至 jniLibs 后 AAR 清空 jni（原版备份 `_backup/hyssl-0.1.11.aar.orig`） |
| `app/libs/hysignal-quic-1.5.113.aar` | 修改 | arm64/v7a 的 libmarsstn.so 已 ELF 裁剪；x86/x86_64 保留原版（不打包）（原版备份 `_ehframe_backup/hysignal-quic-1.5.113_orig.aar`） |
| `app/libs/mmkv-1.0.12.aar` | 修改 | arm64/v7a 的 libmmkv.so 已 stub 化替换（原版 AAR：`app/libs/mmkv-1.0.12.aar.bak`；当前态快照：`_backup/mmkv-1.0.12.aar.orig2`） |
| `app/libs/common-gamesdk-release.aar` | 修改 | jni/ 内 so 已移除（原版备份 `_backup/common-gamesdk-release.aar.orig`） |

---

## 七、待办

- [x] ~~真实 TV 设备回归测试（验证 stub 在 ARM 真机上的 libhyquic 16 个 posix_quic 符号是否完整）~~ → 已通过离线 ELF 校验（marsstn UND vs stub 导出，32/64 位均 0/16 missing）+ 雷电实测确认，见 08-28 记录 11.17.1
- [x] ~~体积优化：评估 libnsdt.so 是否可剔除~~ → 实测**不可 exclude**（NSDT static 块 loadLibrary 无守卫，exclude 会 UnsatisfiedLinkError 崩溃），改用 **JNI stub 替换**成功，见八
- [x] ~~Android 5.1.1 真机测试 libhycrypto / libhyssl 链路~~ → 逍遥 MEmu 5.1.1（x86+Houdini 转译 armeabi-v7a）实测：SDK init 成功、房间数据链路正常，见十
- [x] ~~体积优化：继续评估 libmtpencrypt.so（约 395KB）是否可 stub（mso 加密/解密）~~ → **不再 stub**，改为**整体移除 mtpencrypt-adr-1.0.2.aar** + Java 纯明文替代（NSFunction.encryptBody 等已改造，无 native 依赖），见十二
- [x] ~~体积优化：评估 libhyssl.so（arm64 826KB）stub 可行性~~ → stub v1（空实现）/ v2（转发系统 OpenSSL）**两轮均失败**，定案**恢复真实 BoringSSL + ELF 裁剪**（826KB→690KB），见十一/十三
- [ ] （后续可选）libmarsxlog + libstlport_shared（合计 ~11KB）→ 收益极小，默认不做

---

## 八、N 项：libnsdt.so JNI Stub 化 ✅（2026-08-29 深夜追加）

### 8.1 背景

`libnsdt.so`（arm64 1.03MB / v7a 566KB）为虎牙网络诊断库（NSDT）。待办原计划评估"是否可剔除"。

### 8.2 反编译分析（nsdt-1.1.3）

- `NSDT.java` static 块 `System.loadLibrary("nsdt")` **无守卫** → **不可 exclude**（否则 UnsatisfiedLinkError 崩溃，同 libmmkv 教训）
- native 方法 **8 个**：`init()V`、`ping(...)I`、`traceroute(...)I`、`tcp(...)I`、`detectIP(...)I`、`getIPStatus(...)I`、`getIPRtt(...)I`、`test()V`
- `setCallBack` 是**普通 Java 方法**（仅赋值 sCallBack），非 native，无需 stub
- 回调 `*ReturnFromNative` 由 native 侧触发；stub 不触发 → 失败监控上报链中断（仅监控数据缺失）

### 8.3 引用链分析（code-explorer 子代理全量搜索）

| native 方法 | 调用方 | 路径性质 |
|---|---|---|
| `init()` | `MtpMarsTransporter.initHal()` L120 + `NSDTWrapper` 构造 L91 | 初始化链（void 空实现正常返回） |
| `ping/tcp/traceroute` | `NSDTWrapper` → `NSDetectNetMgr` | 失败监控上报（非业务路径） |
| `detectIP/getIPStatus/getIPRtt/test` | **无外部调用方** | dead code |

→ 结论：**libnsdt.so 可安全 stub**，仅失败诊断监控失效，对拉流/弹幕/登录零影响。

### 8.4 实现

`app/src/main/cpp/nsdt_stub.cpp`：8 个 JNI 符号空实现（`Java_com_huya_mtp_nsdt_NSDT_*`），全部返回 0/void，`init()` 关键成功返回。
`app/build.gradle` jniLibs.pickFirsts 添加 `lib/*/libnsdt.so`（stub 优先于 aar 真实 so）。

### 8.5 验证（雷电 x86_64 转译 armeabi-v7a）

- logcat：`NSDTStub: [STUB] NSDT.init() - noop` —— init 被 stub 拦截成功
- crash buffer 空，无 UnsatisfiedLinkError / dlopen 错误
- `DebugChecker passed=3/3`
- 直播间数据预加载正常（roomId=11342399/11279251 inflight=1）
- 弹幕 SDK 正常：`code=0 alreadyTimeout=false`（无超时）

### 8.6 体积收益

| ABI | 优化前 | 优化后 |
|---|---|---|
| arm64-v8a | 7.40MB | **7.09MB**（libnsdt 1.03MB→5KB） |
| armeabi-v7a | 6.83MB | **6.59MB**（libnsdt 566KB→3.7KB） |

---

## 九、O 项：hydeviceid-1.5.24-np.aar 整体移除 ✅（2026-08-29 追加）

### 9.1 背景

`hydeviceid-1.5.24-np.aar`（2341KB）为虎牙设备指纹 SDK（UDB 指纹上报）。待办评估体积优化，目标移除 `libhydeviceid.so`（arm64 962KB / v7a ~800KB）。

### 9.2 依赖面分析（正确方法：解压 classes.jar 后字节码级扫描）

> ⚠️ 教训：直接读 aar 内压缩的 classes.jar 二进制扫描会**漏检**（zip 压缩流中字符串不可见）。
> 必须 `zipfile` 解压出 class 字节后再扫。

| 引用方 | 类 | 用到的成员 |
|---|---|---|
| huyaudbunify-1.0.80007.1.1.aar | `com.huyaudbunify.HuyaAuth` | `host` 字段(putstatic)、`getInstance()`、`getCDID()`、`addSdidHandler(SdidHandler)` |
| huyaudbunify-1.0.80007.1.1.aar | `com.huyaudbunify.HuyaAuth$2` | 实现 `com.huya.security.SdidHandler` |
| huyaudbunify-1.0.80007.1.1.aar | `com.huyaudbunify.util.HuyaUrlUtil` | `getInstance().getSDID()`（拼接 UDB 请求 URL） |
| app 源码 | `HuyaSDKParser` | `host/kiwiHost/nimoHost/openApiHost` 字段 + `HyDeviceChecker.setUrlDeviceChecker()`（均 try-catch(Throwable) 守卫） |

**关键坑**：原 `DeviceFingerprintSDK` 是单例，`getInstance()` 强制 `init()→workThread.start()` 后台上报；且原 `NativeBridge` static 块 `loadLibrary("hydeviceid")` 用 `catch(Exception)` 守卫——而 `UnsatisfiedLinkError` 是 **Error 不是 Exception**，无法被捕获 → **不可「保留 classes 删 so」**（类初始化必崩），必须整体换 stub 类。

### 9.3 实现

- `app/src/main/java/com/huya/security/DeviceFingerprintSDK.java`：按原类全量 public API stub（含 `getInstance()`/`getInstance(String)`/`getCDID()`/`getSDID()`/`getOldLog()`/`init()`/`addSdidHandler()`/`link()` + 全部静态字段 + `sdidHandlers`），getCDID() 返回固定伪 ID，无任何逻辑。
- `app/src/main/java/com/huya/security/SdidHandler.java`：接口 `void onSdid(String)`。
- `app/src/main/java/com/huya/security/hydeviceid/HyDeviceChecker.java`：`setUrlDeviceChecker(String)` no-op。
- `app/build.gradle` fileTree exclude 添加 `'hydeviceid-1.5.24-np.aar'`（连带去掉 classes.jar 22 类 + assets/oaid.dex）。
- R8 侧原有 `-keep class com.huya.security.**` 保留 stub，无 missing class 报错。

### 9.4 实测（逍遥 MEmu 5.1.1 x86 + Houdini 转译 armeabi-v7a）

**首轮失败**：`NoSuchMethodError: No static method getInstance()Lcom/huya/security/DeviceFingerprintSDK;`
- 堆栈：`com.huyaudbunify.HuyaAuth.init(SourceFile:150)`
- 根因：stub 初版只覆盖了 HuyaSDKParser 的 4 个字段，**漏掉 SDK 内部 `getInstance()` 调用**（HuyaAuth.init 内部实际调用）。HuyaSDKParser 捕获了异常 → app 未崩，但 HuyaBerry init 失败 → SDK 回调永不触发 → 播放器全部"等待超时"。
- 修复：按 9.2 全量 API 重写 stub。

**修复后**（9527 日志）：
```
✅ HuyaBerry SDK 初始化成功 (init 无异常)
✅ HuyaBerry SDK 初始化 & 绑定完成
【App】✅ BerryEvent 事件代理已注册
V33DBG room=11342412 ... bitRateInfoList=[...4 个码率档...]
```
- 无 NoSuchMethodError / NoClassDefFoundError / UnsatisfiedLinkError
- 房间数据链路持续正常（数十个房间 bitRateInfoList/singleStreamInfo）
- H264 软解（OMX.google.h264.decoder）+ AAC 解码器活跃
- 已知非阻塞项：`SecureDataStore AES KeyGenerator not found`（x86 转译环境）
  - ~~自定义源 `diyp.zxyxndc.top` DNS 失败（不计入失效）~~ **✅ 已修复（见十六节）**：该域名已彻底失效（DNS 无记录），
    源仓库删除相关 23 个频道后，app 因命中 jsDelivr 旧缓存键（`@refs/heads/main`）仍拉到旧源；
    修复 `LiveSourceLoader.convertToJsdelivr` 后 DNS 失败 **23 → 0**。

### 9.5 体积收益

| ABI | 优化前 | 优化后 |
|---|---|---|
| arm64-v8a | 7.09MB | **6.72MB**（hydeviceid 962KB→0 + oaid.dex + classes） |
| armeabi-v7a | 6.59MB | **6.31MB**（hydeviceid ~800KB→0 + oaid.dex + classes） |

---

## 十、Android 5.1.1 逍遥回归 ✅

- 设备：逍遥 MEmu（Nubia NX809J 机型壳），Android **5.1.1**，x86 + Houdini 转译 armeabi-v7a
- adb：`127.0.0.1:21513`（实例1，`memuc listvms` 确认）
- 注意：模拟器重启后 `/data/local/tmp` 清空，需重新 `adb push` + `pm install -r`
- 结果：SDK init 成功、房间数据正常、解码器活跃、零崩溃（详见 9.4）

---

## 十一、P 项：libhyssl.so / libhycrypto.so stub 化评估 → 定案恢复真实 BoringSSL ✅

### 11.1 原始评估

| 项 | 内容 |
|---|---|
| 大小 | arm64 **826KB** / v7a ~700KB |
| 依赖 | `libmarsstn.so` DT_NEEDED **libhyssl.so**（dlopen 硬解析）→ 不可 exclude，只能 stub |
| marsstn 引用 | 26 个 SSL 符号（SSL_CTX_new/SSL_new/SSL_connect/SSL_read/SSL_write/SSL_set_fd/TLS_client_method/...） |
| 风险 | mars 长链 TLS 通道失效；若服务端强制 TLS 则拉流/弹幕/登录全挂；若可回退明文则仅 TLS 传输降级 |
| 验证方式 | 仅能实测：stub → 逍遥装机 → 检查 SDK init + 取流 |

### 11.2 两轮 stub 实测 → 均失败

- **v1 空实现 stub**：`SSL_CTX_new` 等返回 NULL/空 → marsstn TLS 建链失败，房间解析超时（>30s），播放不可用
- **v2 转发系统 OpenSSL**（`dlopen("/system/lib64/libssl.so")` + `dlsym`）：Android 8.0+ **linker 命名空间隔离**（app 默认 `"clns-*"` namespace 不可访问系统 libssl/libcrypto），`dlsym` 全部返回 NULL，方案不可行

### 11.3 定案：恢复真实 BoringSSL

- 从 `hyssl-0.1.11.aar`（jni/）提取真实 BoringSSL 到 `app/src/main/jniLibs/`：
  - `libhycrypto.so`：arm64 1683KB / v7a 1154KB
  - `libhyssl.so`：arm64 826KB / v7a 522KB
- 与 libmarsstn（同为 BoringSSL 编译）**符号级完全兼容**，TLS 真实可用
- AAR 中 jni/ 清空（防重复打包），原版备份 `_backup/hyssl-0.1.11.aar.orig`
- 体积反弹 ~2.5MB → 通过 **R 项 ELF 紧凑重写**（见十三）裁回，净效果 arm64 **-1.36MB**

| 库 | arm64 裁剪前→后 | v7a 裁剪前→后 | 说明 |
|---|---|---|---|
| libhycrypto.so | 1683KB→**1511KB** | 1154KB→**1134KB** | jniLibs，ELF 裁剪 |
| libhyssl.so | 826KB→**690KB** | 522KB→**510KB** | jniLibs，ELF 裁剪 |

---

## 十二、Q 项：mtpencrypt-adr-1.0.2.aar 整体移除 ✅（2026-08-29 追加）

- 对象：`mtpencrypt-adr-1.0.2.aar`（535KB，含 `libmtpencrypt.so` v7a 205KB + `EncryptJni/HyEncrypt/BuildConfig` 3 类）
- 依据：mtpencrypt 为 TEA 上报加密（`Content-Encrypt: yyencrypt`），仅被 `NSFunction.encryptBody()` 与 `NetworkUtil.httpRequest()` 引用（上报/辅助请求，纯观看场景失败无害）
- 方案：app 侧提供同名纯 Java 替代（`app/src/main/java/com/huya/mtp/encrypt/`：`keyEncrypt2` 明文直通，服务端解密失败仅使该上报请求无效，不崩溃）
- 注意：**不可「保留 classes 删 so」** —— `EncryptJni.loadLibrary` 用 `try-catch(Exception)` 守卫，而 `UnsatisfiedLinkError` 是 `Error` 无法被捕获 → 调用必崩；故必须整体换 Java 替代
- R8 侧 `-keep class com.huya.mtp.**` 已覆盖。风险：低。

---

## 十三、R 项：ELF 紧凑重写裁剪 ⚠️【结论已失效，勿照搬】（2026-08-29 收尾核心工作）

> ## 🛑 重要勘误（见**十七节**：arm64 裁剪已全部回滚）
>
> 本节记录的 ELF 裁剪**只在雷电（新版转译器）上验证通过**，漏测逍遥2（Android 12 / Houdini 12）。
> 后续实测：**裁剪后的 arm64 库在 Houdini 12 下触发 SIGSEGV（转译器自身崩溃），app 打开即闪退**。
> 详见**十五节**：`libhycrypto` / `libhyssl` / `libmarsstn` 三者中**任一**为 shrunk 版即崩。
>
> **当前状态（以此为准）**：
> - arm64 的三个核心库已**全部还原为原版**（1723360 / 846280 / 1702472），裁剪产物**不再进入 APK**
> - 本节 13.4 的裁剪结果表、13.6 的体积收益**均已作废**
> - 本节的**工具与 bug 修复经验（13.2 / 13.3）仍有参考价值**，但**结论不可复用于 arm64 部署**
>
> 教训：ELF 二进制改写**在不同 Houdini 版本上兼容性不同**（Houdini 4.x 吃 ARM32 shrunk、Houdini 12 拒绝 ARM64 shrunk）。

### 13.1 背景

P 项恢复真实 BoringSSL 后体积反弹 ~2.5MB，且 AAR 内 `libmarsstn.so`（arm64 1663KB）仍是大头。
这些库均为第三方预编译产物（无源码/链接配置），无法重新编译裁剪 → 采用**二进制 ELF 紧凑重写**方案。

### 13.2 工具：`_elf_shrink.py`

纯 Python（pyelftools）手写 ELF 紧凑重写器，不依赖任何链接器，流程：

1. **剥离异常处理表**：删除 `.eh_frame` / `.eh_frame_hdr` section 及 `PT_GNU_EH_FRAME` program header（arm64 Android 使用 DWARF4 全表，纯观看场景无符号回溯需求，可安全删除）
2. **未引用 section 回收**：删除零大小/未使用 section，section header table 紧凑重排
3. **重写元数据**：section offsets、`sh_link`、program headers 全部按新布局重算

### 13.3 关键 bug 修复（本轮最重要）

- 现象：裁剪后 `ldd`/动态加载器的版本检查异常
- 根因：`.gnu.version_d` / `.gnu.version_r` 的 `sh_info` 字段是**版本定义/需求的数量**（不是 section 索引），却被 `remap()` 按索引错误映射成 0
- 修复：只对 `SHT_RELA` / `SHT_REL` / `SHT_GROUP` 类 section 映射 `sh_info`；修复后 `objdump --version-info`（VERDEF/VERNEED）与原始文件**完全一致**

### 13.4 裁剪结果

| 库 | arm64 前→后 | v7a 前→后 | 位置 |
|---|---|---|---|
| libhycrypto.so | 1683KB→**1511KB**（-172KB） | 1154KB→**1134KB**（-20KB） | jniLibs |
| libhyssl.so | 826KB→**690KB**（-136KB） | 522KB→**510KB**（-12KB） | jniLibs |
| libmarsstn.so | 1663KB→**1550KB**（-113KB） | 854KB→**817KB**（-37KB） | hysignal-quic-1.5.113.aar 内 arm64/v7a |

- x86/x86_64 的 libmarsstn.so **保留原版未裁剪**：`build.gradle` splits 仅打 arm64-v8a + armeabi-v7a，x86 系不会进入 APK
- 原版与中间版全部备份于 `_ehframe_backup/`（含 `hysignal-quic-1.5.113_orig.aar`）

### 13.5 验证

1. **符号一致性**：裁剪后 dynsym 导出符号数量/名称与原始一致，VERDEF/VERNEED 完整
2. **双 ABI 部署校验**：jniLibs 4 库 + AAR 内 marsstn，`.dynsym`/`.dynstr`/版本节全部 OK
3. **release 构建成功**：`app/build/outputs/apk/release/tv_live_release_{arm64-v8a,armeabi-v7a}_2.0.179.apk`
4. **模拟器实测**（雷电 x86_64 转译）：SDK 初始化成功、房间解析 `code=0`、播放 `STATE_READY`、零崩溃

### 13.6 最终体积

| 版本 | arm64-v8a | armeabi-v7a |
|------|-----------|--------------|
| **最终 release** | **5.37MB** | **5.14MB** |
| 本日初始（修复 mmkv/hycrypto 后） | 7.73MB | 7.13MB |
| 净缩减 | **-2.36MB** | **-1.99MB** |

### 13.7 遗留说明 ⚠️【本节末条已被推翻】

- `libmarsxlog.so`（9KB）+ `libstlport_shared.so`（2KB）**未裁剪**：极小，且无 `.eh_frame` 可删，收益为零，刻意保留
- ~~本日结束点即当前 git 工作区状态：8 个部署 so（jniLibs 4 + AAR 内 arm 2 + x86 2 原版）全部验证通过~~
  **❌ 已推翻**：所谓"全部验证通过"只测了雷电。逍遥2（Houdini 12）实测 arm64 裁剪库必崩，
  三个 arm64 核心库已还原原版。当前真实状态见**十七节 SO 总表**。

---

## 十四、逍遥 MEmu 5.1.1（x86+Houdini）SDK init 失败根因与修复 ✅（2026-08-29 凌晨追加）

> 承十三节：R 项裁剪验证**只测了雷电**（x86_64 新版转译器），漏测逍遥（Android 5.1.1 + 老 Houdini 4.x 转译 armeabi-v7a）。
> 逍遥上 2.0.179 复现：`c++_shared` / `marsstn` 报 `unexpected e_machine: 40`，SDK init 失败。

### 14.1 现象（逍遥 127.0.0.1:21513，Android 5.1.1，Houdini 转译）

```
D/houdini: Load library .../libc++_shared.so failed.          ← Houdini 主动拒绝
E/art: dlopen failed: ".../libc++_shared.so" has unexpected e_machine: 40
D/houdini: Added shared library .../libstlport_shared.so for ClassLoader by Native Bridge.  ← 小库被接管
D/houdini: Load library .../libmarsstn.so failed.              ← 连带失败
E/art: dlopen failed: ".../libmarsstn.so" has unexpected e_machine: 40
```

- 成功：`stlport_shared`（1.2KB stub）/ `tvlive_security`（11KB）
- 失败：`c++_shared`（291KB）/ `marsstn`（854KB）

### 14.2 根因定位（备份正式版对比法）

1. 备份正式版 `AH-main1\_optimized_backup\NEW_apk.apk`（08-25 加固版）装逍遥实测：**全功能正常**（SDK init ✓ / 预解析 ✓ / 播放 READY ✓）
2. 对比两 APK 的 `lib/armeabi-v7a/libc++_shared.so`：
   - **备份 = 657000 字节**（NDK 标准，`e_flags=0x5000200` = EF_ARM_ABI_VER5）
   - **当前 = 291920 字节**（AGP strip 后；源自 `mmkv-1.0.12.aar` 内 `jni/armeabi-v7a/libc++_shared.so` 504088，`e_flags=0x200034` = **EF_ARM_ABI_VER4**，旧 NDK 产物）
   - `jniLibs.pickFirsts` 有 `lib/armeabi-v7a/libc++_shared.so`，但**无同名 jniLibs 文件时 pickFirst 选中了 mmkv AAR 的旧版**
3. 依赖链：`libmarsstn.so` 的 `DT_NEEDED` 硬依赖 `libc++_shared.so` → c++_shared 加载失败 → **marsstn 连带失败** → SDK init 失败
4. **结论**：Houdini 4.x 拒绝转译 mmkv AAR 自带的旧版 c++_shared（EF_ARM_ABI_VER4 结构异常）；雷电新版转译器接受，故雷电正常

### 14.3 修复（已验证）

- 将 NDK 标准 `libc++_shared.so`（657000，取自备份正式版，实测 Houdini 可接管）放入
  `app/src/main/jniLibs/armeabi-v7a/libc++_shared.so`（jniLibs 优先级 > AAR）
- 重新构建：v7a APK 内 c++_shared = **657000** ✓（arm64 包未变，雷电不受影响）

### 14.4 验证（逍遥实测）

- Houdini 日志：**9 个 ARM 库全部 `Added by Native Bridge`**，零 failed、零 e_machine
- SDK init 成功、SecurityCore 正常、预解析 54 房间全部成功（`streams=2/3`）、Mars 网络栈活跃
- 与备份正式版行为一致

### 14.5 遗留说明

- ~~`arm64-v8a` 包的 c++_shared 仍为 mmkv AAR 版（429KB，strip 后）；雷电/真机正常。若需彻底统一，可从 NDK r25 标准版替换 `jniLibs/arm64-v8a/`~~
  **✅ 已处理**：arm64 的 c++_shared 已换为 NDK 标准版 **1058904**（取自 `_so_extract/libc++_shared.so`，
  ELF64 AArch64 / e_flags=0x0）。注意本机 NDK 实为 **27.0.12077973**（非 r25）。详见十五节。
- 教训：**模拟器兼容性验证不能只测新模拟器**；Houdini 4.x（5.1.1）对旧 NDK ELF（EF_ARM_ABI_VER4）与 R 项裁剪 ELF 均敏感，回归时优先用逍遥

---

## 十五、逍遥2（Android 12, x86_64+Houdini 12）打开即闪退根因与修复 ✅（2026-08-29 追加）

> 逍遥2 = 另一台逍遥实例（127.0.0.1:21523，NX809J，**Android 12**，x86_64 CPU，Houdini 12.0.0_z.38816.m RELEASE 转译 arm64-v8a）。
> 现象：安装 arm64-v8a 正式包后**打开即闪退**（反复自动重启仍闪退）。

### 15.1 现象（logcat）

```
F libc: Fatal signal 11 (SIGSEGV), code 2 (SEGV_ACCERR), fault addr 0xc957e80 in tid (huya-sdk-init), pid (com.tv.live)
F DEBUG: backtrace:
  #00 pc 0x12fcc3 libhoudini.so
  #01 pc 0x1d591f libhoudini.so
  #02 pc 0xf4716  libhoudini.so
  #03 pc 0x2f1680 libhoudini.so
```

- 崩溃线程恒定 `huya-sdk-init`（SDK 初始化线程）
- **fault addr 偏移恒定 0x57e80**（基址随重载变化，偏移不变）→ 确定性崩溃
- 栈全在 `libhoudini.so` 内 → **Houdini 12 转译器自身崩溃**
- 崩溃发生在 SDK init 极早期（无任何 SDK 日志）

### 15.2 排除定位（组合替换法）

| 测试包 | c++_shared | hycrypto | hyssl | marsstn | 结果 |
|---|---|---|---|---|---|
| 原版 2.0.179 | 429K (mmkv AAR) | 1546K shrunk | 706K shrunk | 1586K shrunk | **闪退** |
| 仅换 c++_shared | 1058K 标准 | shrunk | shrunk | shrunk | **闪退**（fault 同偏移） |
| 全原版 | 1058K 标准 | 1723K 原版 | 846K 原版 | 1702K 原版 | **正常**（进程存活/9527/SDK init/预解析 24 条） |
| comb1 | 标准 | 原版 | 原版 | shrunk | **闪退** |
| comb5 | 标准 | shrunk | shrunk | 原版 | **闪退** |

**结论**：R 项 ELF 紧凑裁剪（shrunk）的 arm64 库——**hycrypto / hyssl / marsstn 三者中任一存在，都会在 Houdini 12 转译时触发 SIGSEGV**。
（对比：v7a 的 shrunk 库在逍遥1 Houdini 4.x 上正常，ARM32 裁剪可接受；ARM64 裁剪被 Houdini 12 拒绝。）

### 15.3 修复（已验证）

`app/src/main/jniLibs/arm64-v8a/` 全部换回原版/标准库（jniLibs 优先级 > AAR）：

| 库 | 替换后大小 | 来源 |
|---|---|---|
| libc++_shared.so | 1058904（NDK 标准） | `_so_extract\libc++_shared.so` |
| libhycrypto.so | 1723360（原版） | `_so_extract\libhycrypto.so` |
| libhyssl.so | 846280（原版） | `_so_extract\libhyssl.so` |
| libmarsstn.so | 1702472（原版） | `_so_extract\libmarsstn.so` |

（v7a 不动：jniLibs/armeabi-v7a 保持"标准 c++_shared + shrunk 库"，逍遥1 已实测正常。）

### 15.4 验证（逍遥2 实测）

- 进程存活、无 signal 11、9527 端口监听
- 虎牙 SDK 初始化完成 ✓ / SecurityCore ✓ / DEX 保护 ✓ / loadLibrary(marsstn/stlport/c++_shared) ✓ / StnLogic.startTask ✓
- 预解析 24 条成功

### 15.5 遗留说明

- arm64 shrunk 库的 ELF 细节（相对 v7a 被 Houdini 12 拒绝的具体结构）待深挖；当前以原版库回归为准
- APK 体积回归：arm64 包 +430KB（原版 4 库 vs shrunk 4 库）
- 教训补全：**同一 ELF 处理在不同 Houdini 版本上兼容性不同**（Houdini 4.x 吃 ARM32 shrunk、Houdini 12 拒绝 ARM64 shrunk）；正式回归矩阵应含逍遥 5.1.1 / 逍遥 Android 12 / 雷电 / 真机

---

## 十六、直播源拉取命中 jsDelivr 旧缓存（DNS 失败 23 条）根因与修复 ✅（2026-08-29）

### 16.1 现象

- 逍遥1（5.1.1）首次安装后，9527 日志出现 **23 条**：
  `检测异常(不计入失效): http://cdn.jdshipin.com:8880/huya.php?id=xxx → Unable to resolve host "diyp.zxyxndc.top"`
- 该域名 `nslookup` 实测 **Non-existent domain**（第三方接口已彻底失效），23 条全在「虎牙影视」分组
- 用户在自己仓库删除这 23 条后，**app 重装拉到的源仍逐字节相同**（39410 B / 23 条 jdshipin / 213 频道）

### 16.2 根因（两层）

**第 1 层：jsDelivr 对两种 ref 写法是两套独立缓存**

| jsDelivr URL | 大小 | jdshipin | 频道 |
|---|---|---|---|
| `@refs/heads/main`（app 原用） | 33686 | **23** | 213（旧快照）|
| `@main` / 无 ref | 30136 | **0** | 190（最新）|

app 的 `LIVE_URL` 为 `https://raw.githubusercontent.com/cuicanrensheng/IPTV/refs/heads/main/playlist1.m3u`，
经 `LiveSourceLoader.convertToJsdelivr()` 转换后用的是 `refs/heads/main`，**命中未刷新的旧缓存键**。

**第 2 层：`parseGitHubUrl` 的 split 截断**

```java
String[] parts = pathPart.split("/", 4);
// "cuicanrensheng/IPTV/refs/heads/main/playlist1.m3u"
//   → ["cuicanrensheng","IPTV","refs","heads/main/playlist1.m3u"]
info.branch = parts[2];   // = "refs"   ← 只取到第一段
info.path   = parts[3];   // = "heads/main/playlist1.m3u"
```
故只对 `info.branch` 做 `refs/heads/` 剥离**不会命中**（branch 是 `refs`）。

### 16.3 修复

1. `LiveSourceLoader.convertToJsdelivr()`：**branch 与 path 先合并再规范化**
   `branch + "/" + path` = `refs/heads/main/playlist1.m3u` → 剥前缀 → `main/playlist1.m3u`
   → 最终 `https://cdn.jsdelivr.net/gh/cuicanrensheng/IPTV@main/playlist1.m3u`
2. 新增 `normalizeBranch()`：剥离 `refs/heads/`、`refs/tags/`
3. `SourceHealthChecker`：
   - `setInstanceFollowRedirects(false)`：只验源首跳（302 即视为可用），不再解析跳转目标域名
   - 新增 **DNS 失败负缓存**（`dnsFailHosts`，按 host 记 30 分钟），`UnknownHostException` 后直接跳过，不再空等超时
4. `gradle.properties`：`org.gradle.jvmargs` 4096m → **2048m**（构建报 `errno=1455` 页面文件不足）

### 16.4 验证（逍遥1 重装实测）

- 下载 URL → `...@main/playlist1.m3u` ✓，size **30136**，频道 **185**
- **DNS 失败 23 → 0** ✓
- 兜底文件 `live_source_fallback.m3u`：jdshipin **0**、190 频道
- 无 native 崩溃

### 16.5 卡顿复测结论（重要：卡顿与 DNS 无关）

| 指标 | 修复前 | 修复后 |
|---|---|---|
| DNS 失败 | 23 | **0** |
| 解码 CPU（MediaCodec_loop + h264.decoder） | 52% + 18% | 50% + 15% |
| Idle（启动 32s 采样） | 8% | 8% |
| 主线程掉帧日志 | 无 | 无 |

**画面卡顿主因是模拟器无硬解**（`OMX.google.h264.decoder` 软解占约 65% CPU），与 DNS 无关；真机有硬解不受影响。
预解析 1.5s/房间的间隔是刻意节流（`HuyaSDKParser.PRELOAD_INTERVAL_MS = 1500L`），非 DNS 导致。

### 16.6 待办（卡顿优化，未做）

1. 软解环境下首播默认选低码率档（避开 4000bps 蓝光）
2. 预解析错峰：播放稳定后再启动，避开启动瞬间 CPU 争抢

---

## 十七、⚠️ 当前状态总表与历史勘误索引（截至 2026-08-29 收尾，**以此节为准**）

> 前面各节是按时间顺序写的流水账，其中若干结论已被后续实测推翻。
> **查询"现在到底是什么状态"时，一律以本节为准**，不要直接引用十三/十四/九节的旧结论。

### 17.1 当前 SO 全景（arm64 / v7a 两包一致）

**✅ 未修改的 SDK 原版（5 个）**

| SO | arm64 / v7a | 功能 |
|---|---|---|
| `libmarsstn.so` | 1723360 / 874596 | Mars STN 网络长连接/信令——虎牙 SDK 核心网络栈 |
| `libhycrypto.so` | 1723360 / 1181868 | 虎牙加解密/请求签名 |
| `libhyssl.so` | 846280 / 534392 | 虎牙 SSL/TLS 安全通信（真实 BoringSSL）|
| `libmarsxlog.so` | 9968 / 8392 | Mars xlog 日志 |
| `libstlport_shared.so` | 2264 / 1612 | STLPort C++ 运行时 |

**🔧 被动过（6 个）**

| SO | 大小 | 性质 | 原因 |
|---|---|---|---|
| `libc++_shared.so` | 1058904 / 657000 | 换 NDK 标准版 | 原 mmkv AAR 旧 NDK 产物被 Houdini 4.x 拒绝 |
| `libtvlive_security.so` | 16400 / 11796 | **项目自实现** | `security.cpp` + `aes.cpp`，非 SDK 组件 |
| `libmmkv.so` | 8680 / 6024 | **stub** | 弹幕 SDK 强制 `loadLibrary("mmkv")` 且调用链无 try-catch；本 app 不真用 MMKV |
| `libhyquic.so` | 5040 / 3696 | **stub** | QUIC 未启用 |
| `libnsdt.so` | 5056 / 3716 | **stub** | 网络诊断未启用 |
| `libudbauthunify.so` | 4528 / 3268 | **stub** | 统一鉴权未启用 |

> 四个 stub 均由 `app/src/main/cpp/` 下自编译（见 `CMakeLists.txt`），非 SDK 产物。

### 17.2 已失效的历史结论（❌ 勿再引用）

| 位置 | 旧结论 | 现状 |
|---|---|---|
| 十三节 13.4 / 13.6 | ELF 裁剪收益表、体积净缩减 -2.36MB | **已作废**：arm64 裁剪库在 Houdini 12 下必崩，已全部还原原版 |
| 十三节 13.7 | "8 个部署 so 全部验证通过" | **已推翻**：只测了雷电，漏测逍遥2 |
| 十四节 14.5 | "arm64 c++_shared 仍为 mmkv AAR 版 429KB" | **已处理**：现为标准版 1058904 |
| 九节 9.4 | "`diyp.zxyxndc.top` DNS 失败（非阻塞，不计入失效）" | **已修复**：DNS 失败 23 → 0（源删除 + jsDelivr URL 修复）|

### 17.3 设备测试矩阵（复测基准）

| 设备 | 地址 | 系统 | 解码 | 结果 |
|---|---|---|---|---|
| 逍遥1 | 127.0.0.1:21513 | Android 5.1.1（x86+Houdini 4.x，单核）| 软解 65% CPU | 可播，但**卡**（硬件限制）|
| 逍遥2 | 127.0.0.1:21523 | Android 12（x86_64+Houdini 12，2 核）| 软解 | **黑屏**（未解决）|
| 真机 | 127.0.0.1:5555 | REDMI Android 14 | **硬解 3.3% CPU** | ✅ 流畅 1080P，0.67s 就绪 |
| 雷电 | — | 未连接 | — | 回归时需先启动（勿把 5555 当雷电）|

**卡顿结论**：非 app 缺陷。真机硬解仅 3.3% CPU、90 秒零中断；逍遥1 无硬解才卡。

### 17.4 环境事实（避免重复踩坑）

- 本机 NDK = **27.0.12077973**（不是 r25）
- `5555` / `emulator-5554` = **红米真机**（Android 14），**不是**雷电模拟器
- 逍遥1 的 Android 5.1.1 **无 `nproc`**；Android 12+ 的 `top` **不支持 `-t`**（用 `-H`）
- `toybox wget` 在两台逍遥上均不存在，设备内取 URL 内容需另想办法
- Gradle 内存已从 `-Xmx4096m` 降到 **2048m**（4096 会触发 `errno=1455` 页面文件不足）

### 17.5 仍未解决

- **逍遥2 黑屏**：主播放列表 200/1028 字节正常，解码器已创建，但分片数据拉不到 → 一直 `STATE_BUFFERING`。
  真机同 URL 正常播出，属逍遥2 模拟器网络/转译环境问题（待查：网络模式 / 代理 / 换源对比）

---

## 十八、关闭 hyns 网络请求统计上报（NSStatUtil）✅（2026-08-29）

### 18.1 发现过程

在 `D:\ASDF\反编译工具\huya_sdk\mars_decompiled\sources\`（Mars/hysignal Java 层反编译，64 个 .java）
中定位到第四条上报通道——**它与已关闭的三条完全独立**：

```java
// com/huya/mtp/hyns/stat/NSStatUtil.java（来自 hynssignal-1.9.105-exvolley.aar）
public static boolean mEnabled = true;                 // 默认开启

public static boolean shouldReport(NSStatData stat) {
    if (TextUtils.isEmpty(stat.path) || stat.dataType != DataType.wup) {
        return true;
    }
    return true;                                        // 两分支恒 true，无过滤
}
```

`NSStatReporter` 对**每一个网络请求**上报（走虎牙自有通道）：

| 字段 | 含义 |
|---|---|
| `path` / `authority` / `scheme` | 请求路径、主机、协议 |
| `responseTime` / `suspendTime` | 响应耗时、挂起时长 |
| `success` / `retCode` / `errorInfo` | 成败、错误码、错误信息 |
| `requestSize` / `responseSize` | 请求/响应大小 |

**内容仅为网络性能指标，不含 IMEI/OAID/位置/通讯录等隐私。**

### 18.2 为什么之前没关

`文档/_gen_md_part1.ps1` 内嵌的调研结论（6.1/6.2 节）已确认：
**`HuyaBerryConfig.Builder` 只有 12 个方法，不存在 `isOpenStat`/`isOpenAnalytics`/`isOpenApm` 开关**。
因此本项目已有的关闭体系走的是另外三条路：

| 通道 | 关闭方式 | 时机 |
|---|---|---|
| Bugly 崩溃 | `builder.isOpenBugly(false)` | init 前 |
| ciku APM | `MonitorCenter.getInstance().stopReport()` | init 后 |
| Hiido 运营统计 | `BaseApi.setReportApi(new NoOpReportApi())` + `HuyaStatisAgent.mApi`→NoOp | init 后 |

**`NSStatUtil` 的静态字段不在这三条链路内，一直默认开启且无人处理**
（改前全工程搜 `NSStat*` / `mEnabled` 零匹配）。

### 18.3 修复（直接调用，非反射）

`NSStatUtil` 在编译期可见（`hynssignal-1.9.105-exvolley.aar`，maven 依赖，release 包内该类未混淆），
故与既有写法保持一致，用**全限定名直接赋值**，不用反射：

`app/src/main/java/com/tv/live/util/HuyaSDKParser.java`（SDK init 成功后的关闭流程内新增 Step5）：

```java
boolean nsStatOk = false;
try {
    com.huya.mtp.hyns.stat.NSStatUtil.mEnabled = false;
    nsStatOk = !com.huya.mtp.hyns.stat.NSStatUtil.mEnabled;
} catch (Throwable e) { ... }
```

同时纳入 `allOk` 汇总与 `[SDK上报验证]` 输出（新增「hyns 网络请求统计」一行）。

### 18.4 验证（真机 REDMI Android 14）

```
[SDK上报验证] Step5 NSStat 网络统计: mEnabled=false ✅ 已关闭
[SDK上报验证] ====== 全部关闭成功 ✅✅✅ ======
```

五条通道全绿：ciku APM ✅ / Hiido 统计（ReportApi + HuyaStatisAgent）✅ / Builder 精简开关 ✅ /
播放数据·游戏账号 ✅ / **hyns 网络统计 ✅**。

**功能未受影响**：频道 185、虎牙 SDK 初始化完成、预解析 19/55 全部成功、1080P 播放 `STATE_READY`。

### 18.5 ⚠️ 与反编译文档结论的差异（实测修正文档）

`huya_sdk/文档/SUMMARY.md` **9.4 节「剩余未拦截项（建议保留）」**原文：

| 模块 | 位置 | 说明 |
|---|---|---|
| `NSStatReporter` | `hynsstat` | MTP 网络统计，**网络栈运行时必需** |
| `MsgReport` | `huyaudbunify` | 删除会导致登录链路异常 |
| `WupMetricReport` | gamesdk | WUP 协议级指标 |

> 文档结论：「以上**无法通过 NoOp 方式安全关闭**，需字节码级 patch（属深度改造，**不建议动**）」

**本次实测推翻了该保守结论**：`NSStatUtil.mEnabled = false` 属纯统计旁路（不参与协议/连接任何环节），
关闭后 SDK 初始化✅、预解析 19/55 全成功✅、1080P 播放 READY✅，**功能零影响**。
文档当时未做关闭实测，判断偏保守。

（`MsgReport` / `WupMetricReport` 本次未动，仍维持文档的建议保留结论。）

### 18.6 备注（非本次改动引起）

测试期间观测到 `Cache API 请求失败，响应码：503` 多次重试，以及几次「暂停播放 → 重新 READY」。
该 503 **在关闭 NSStat 之前就已存在**（见 16 节日志），属虎牙服务器侧问题，与本次改动无关。

### 18.6 反编译资料位置（便于后续排查）

- SO 原始二进制：`huya_sdk\extracted\<aar>\jni\<abi>\`（含未 stub 的 hyquic 3.91MB / udbauthunify 4.19MB / nsdt 1005KB / mmkv 81.76KB）
- Mars Java 层反编译：`huya_sdk\mars_decompiled\sources\`（64 个 .java）
- 调研文档：`huya_sdk\文档\_gen_md_part1.ps1`（Builder 开关真相、SDK init 内部上报行为表）
- ⚠️ `libmarsstn.so` **没有反汇编产物**，只有二进制，如需汇编需自行 objdump/Ghidra

---

## 十九、电视（创维 Coocaa）推送测试 + EncryptedStorage IV 兼容性修复 ✅（2026-08-29 收尾追加）

### 19.1 电视设备环境

| 项 | 内容 |
|----|------|
| 地址 | `192.168.1.7:5555`（adb connect，局域网） |
| 设备 | Skyworth 3T223_P31（创维/酷开 Coocaa，mt5862 芯片） |
| 系统 | Android 9（API 28），**armeabi-v7a**（32 位） |
| 安装包 | `tv_live_release_armeabi-v7a_2.0.179.apk`（v7a 正式版） |
| 日志通道 | `adb forward tcp:19527 tcp:9527` + `http://127.0.0.1:19527/api/logs`（Token `cec70717c25cab33a26f362a74fe1b18`） |

> ⚠️ 教训：`adb install -r` 覆盖安装后 app 可能仍是旧版（用户反馈），**电视上必须 `uninstall` + `install` 全新安装**。

### 19.2 发现：EncryptedStorage 加密失败（全新安装必现）

- 日志：`[EncryptedStorage] 加密失败: Caller-provided IV not permitted` ×3（每次启动）
- 来源：`HuyaCredentials.storeDefaultCredentials()` 恰好 3 次写操作（`putInt(gameId)` + `putString(appId)` + `putString(appKey)`）
- **根因**：Android Keystore 生成的 AES/GCM 密钥默认 `randomizedEncryptionRequired=true`，
  `Cipher.init(ENCRYPT_MODE, secretKey, gcmSpec)` 传手动 IV 会抛 `Caller-provided IV not permitted`
  （解密模式 DECRYPT_MODE 允许传 IV，故解密从未报错、问题被掩盖）
- **后果**：encrypt() 失败后返回明文降级存储 → 凭证实际以**明文**落盘，加密保护失效（功能不受影响）

### 19.3 修复（`app/src/main/java/com/tv/live/util/EncryptedStorage.java`）

```java
// 修复前（报错）
byte[] iv = new byte[GCM_IV_LENGTH];
new SecureRandom().nextBytes(iv);
cipher.init(Cipher.ENCRYPT_MODE, secretKey, gcmSpec);  // Keystore 禁止手动 IV

// 修复后
cipher.init(Cipher.ENCRYPT_MODE, secretKey);  // 由 Keystore 生成随机 IV
byte[] iv = cipher.getIV();                   // 取出实际 IV 用于存储
```

- 解密逻辑、密文格式（`Base64(IV):Base64(Ciphertext+Tag)`）不变 → 旧数据兼容
- `isKeyValid()` 的加解密自检也随之恢复正常

### 19.4 验证（电视重装 + 9527 日志）

- 重建 release（arm64 5792KB / v7a 5416KB，08:55 构建）→ 卸载重装 v7a 包
- **IV 失败 3 → 0**，`EncryptedStorage 初始化成功` ✅
- SecurityCore native 解密 4 项 OK ✅ / 直播源 185 频道 ✅ / 虎牙分组 `LiveListByTag code=0` ✅
- SDK 上报 NoOpReportApi 已生效、StnLogic.startTask native 注册通过 ✅
- 剩余 warn 均为预期：LIVE_URL_2 404 自动迁移 gitee、VPN/模拟位置检测（仅记录）、无 READ_LOGS 跳过 logcat

---

## 二十、预解析错峰启动（十六.6 待办：首播与预解析抢 CPU）✅（2026-08-29 追加）

### 20.1 背景

十六.6 待办：预解析在启动瞬间与首播抢 CPU（软解设备解码占 ~65% CPU，模拟器实测），
拖慢首帧出画。改为「播放稳定后再启动预解析」。

### 20.2 方案（HuyaSDKParser + TVPlayerManager）

预解析不再随 SDK init 立即开跑，改为**两个触发条件先到先得**：

| 条件 | 触发 | 效果 |
|---|---|---|
| ① 首播就绪 | TVPlayerManager `STATE_READY` → `HuyaSDKParser.notifyPlaybackSettled()` | 立即放行预解析队列 |
| ② 兜底超时 | init 完成后 20s（`PRELOAD_DEFERRED_DELAY_MS=20000`）仍未就绪 | 用户未播放/播放失败时也放行，缓存预热不饿死 |

实现要点（`HuyaSDKParser.java`）：
- 新增 `sPlaybackSettled`（volatile，幂等）+ `notifyPlaybackSettled()`（public，TVPlayerManager 调用）
- 新增统一启动闸门 `maybeStartPreload(reason)`：`sInitOk && !sPreloadScheduled && 队列非空 && sPlaybackSettled` 才提交 `PRELOAD_RUNNABLE`
- `preloadRooms()` 三条分支（init 前 / 待启动 / 在跑）全部改走闸门；init 前入队语义不变
- init 成功补发块不再直接 `post(PRELOAD_RUNNABLE)`：改挂 `PRELOAD_FALLBACK_START` 兜底定时器（20s），
  并调 `maybeStartPreload("init补发")`——覆盖「播放早已稳定（先播普通频道）→ SDK init 后到」的边界情况
- 兜底触发后置 `sPlaybackSettled=true`，后续 `notifyPlaybackSettled` 幂等跳过

`TVPlayerManager.java`：`onPlaybackStateChanged` 的 `STATE_READY` 分支（updateWakeLock 后）调用
`HuyaSDKParser.notifyPlaybackSettled()`（幂等，重复 READY 无副作用）。

### 20.3 效果

- 启动瞬间：0 个预解析请求，首播独占 CPU/网络 → 软解设备首帧更快出画
- 首播就绪后：预解析按原节奏（1.5s/房间）开始预热，点频道命中缓存体验不变
- 不播放场景：20s 后兜底预热，不劣化于原行为

### 20.4 验证

- `:app:compileReleaseJavaWithJavac` BUILD SUCCESSFUL
- 待装机回归日志关键行：`🎬 首播已就绪 → 错峰启动预解析`（或 `⏰ 兜底启动`）、
  启动至 READY 之间无 `🔁【预解析】` 条目

> ⚠️ 注意：本次会话发现 `local.properties` 开头有 BOM 导致 `sdk.dir` 失效
> （git bash 下报 SDK location not found），构建需 `ANDROID_HOME=C:\Users\16937\Android` 或重建该文件。

---

## 二十一、直播源提速 + 旧链接清理 + 多端回归（2026-08-29 下午追加）

> 本节涵盖：9527 日志通道排障 / 预解析错峰装机验证（电视+雷电）/ 首装提速（镜像竞速→源1换 gitee）/
> UrlConfig + SourceManager 旧链接清理 / 逍遥三版本回归 / 电视出画慢的根因定界

### 21.1 9527 日志通道排障（重要环境教训）

| 现象 | 根因 | 解决 |
|---|---|---|
| 电视/雷电 9527 空响应（curl exit 52） | **对 9527 用了 `adb reverse`**：adbd 在设备端监听 9527（root 下按 inode 定位确认），把应用 LogServer 顶掉，且 adbd 把连接转发回主机 9527（无人监听）→ 空响应 | **拉日志只用 `adb forward`，永远不要对 9527 用 reverse**；已占用时需 `reverse --remove-all` + force-stop 重启应用重新绑端口 |
| 9527 被占时应用侧表现 | LogServer 绑定失败仅日志一行，应用功能正常 | — |

### 21.2 预解析错峰装机验证 ✅（二十节实现，本节验证）

- **电视（创维 3T223_P31，Android 9，v7a）**：`[PLAY] 播放就绪 (STATE_READY)` 与 `🎬 首播已就绪 → 错峰启动预解析` 同秒出现，READY 前预解析请求 **0 条**；READY 后 `🚀(init补发)` 立即放行（init 恰在 READY 后 3s 完成）
- **雷电（x86_64 转译 arm64）**：同上 ✅
- 三台逍遥（5.1.1/9/12）：全部 ✅（见 21.6）
- 失败房间不阻塞：预解析 code=1 的房间只影响自身，不影响闸门时序

### 21.3 首装黑屏优化：源1 换 gitee + 镜像竞速（演进过程）

**问题**：电视首装黑屏 15s。分段实测：应用侧初始化 ≤1s、源下载（GitHub raw）5~18s、默认频道（江西移动 IPTV 中继 hwrr.jx.chinamobile.com）TTFB 6~11s。

**尝试1（LiveSourceLoader 镜像竞速，保留）**：
- 源1（GitHub）的 jsDelivr/gitmirror/raw 三镜像并发竞速，先返回可解析内容者胜；超时转源2，未完成镜像**后台继续跑完并写缓存**（下次启动命中）
- 实测教训：① gitmirror 在电视 DNS 解析失败（UnknownHostException，瞬间失败不占时）；② **时限 8s 会误杀 jsDelivr**（首次 TLS 握手 15s，日志 `InterruptedIOException: interrupted`）；③ 时限 15s 又拖慢源2 上场——最终定 **5s**：源1 没戏就立刻转源2，慢镜像后台落缓存
- ⚠️ 证书学习澄清：`SecurityCertificatePinner`（TOFU）工作在 TLS 握手**之后**（毫秒级算指纹），`首次学习证书` 日志只是握手完成的时间戳，**不是慢的原因**，勿再误判

**尝试2（终案，用户指定）**：源1 直接替换为 gitee 地址 `https://gitee.com/qf_1111/iptv/raw/master/dfdvfaf.m3u`（185 频道，1s 返回）。gitee 直连比任何 GitHub 镜像都快且稳，竞速逻辑成为死分支（isGitHubUrl 不命中自动跳过，保留给未来自定义 GitHub 源）。

**效果对比（首装→出画）**：串行 jsDelivr ~24s+ → 竞速+源2 11s → 换 gitee 直连 **5s**（电视，受中继波动 5~13s）

### 21.4 旧链接清理（UrlConfig + SourceManager）

- **UrlConfig.java**：删除源1/源2 的 AES 密文（B_LIVE_1/B_LIVE_2）、`getLiveUrl/getLiveUrl2` 改为明文常量直返、删除全部「解密后迁移」逻辑与 `sanitizeLiveUrl()` 方法（含 iptvedqu.m3u 404 黑名单、GitHub 旧地址替换）
  - 当前源：源1 `dfdvfaf.m3u`（185 频道）/ 源2 `playlist.m3u`（125 频道），均 gitee
  - EPG 两段密文与 native 加密链路**未动**（RAW[E1/E2]=ok 验证正常）
- **SourceManager.java**：删除三处 `sanitizeLiveUrl` 调用与「失效地址强制迁移」块、`sanitizedAnyUrl` 死标记
- LiveSourceLoader 的 GitHub 识别/镜像转换是通用功能代码（用户自定义 GitHub 源仍可用），**保留**
- 全工程 grep 确认无 raw.githubusercontent/iptvedqu 业务硬编码残留

### 21.5 多端冷启动出画实测（同版 APK 2.0.179）

| 设备 | Android/ABI | 场景 | am start → READY | UI TotalTime | 备注 |
|---|---|---|---|---|---|
| 雷电 (127.0.0.1:5555) | 14 / arm64 转译 | 首装 | **3s** | 572ms | 源1 1s 出 185 频道 |
| 雷电同上 | 14 | 冷启动(有缓存) | **1.5s** | 334ms | 播放器预热 119ms |
| 逍遥1 (:21513) | **5.1.1** / x86+Houdini4 | 首装 | **4s** | 330ms | v7a 包 |
| 逍遥0 (:21503) | **9** / x86_64 | 首装 | **~2s** | 384ms | arm64 包 |
| 逍遥2 (:21523) | **12** / x86_64+Houdini12 | 首装 | **3s** | 455ms | arm64 包；**17.5 黑屏未复现，建议销项** |
| 创维电视 (192.168.1.7) | 9 / v7a 原生 | 冷启动(有缓存) | **~7.5s** | 947ms | 应用侧 ≤1s，剩 6s 见 21.7 |

所有设备：0 错误 / 0 崩溃 / 0 UnsatisfiedLinkError / dlopen 全过 / 错峰验证通过。

### 21.6 电视出画慢根因定界（重要结论）

- PC 直连江西中继实测 **TTFB 45~64ms**（3 次稳定）→ **服务器不慢**
- 雷电（走 PC 网络）1.5s vs 电视 7.5s → 差异在**电视侧链路**（疑 WiFi 信号差/网线协商 百兆+误码）
- **百兆网卡带宽本身不是瓶颈**：8M 码率流仅 1MB/s，30KB 播放列表 <0.1s；TTFB 慢是 RTT/丢包放大，非带宽
- 应用侧开销 ≤1s 已到头；下一步唯一抓手：电视插网线/换网线（物理层）或换默认频道（产品决策）
- 电视 adb 会随待机关闭（`积极拒绝`），ping 通≠adb 通；唤醒电视后重连即可

### 21.7 环境备注更新

- `local.properties` 已重写为正斜杠（修复 BOM + 转义问题），现 git bash 直接可构建
- 电视日志通道：`adb forward tcp:19527 tcp:9527`；逍遥三实例：19527/19528/19529 → 各自 9527
- memuc 路径 `D:\Program Files\Microvirt\MEmu\memuc.exe`；逍遥实例 adb 端口 21503(实例0)/21513(实例1)/21523(实例2)，对应 Android 9/5.1.1/12

### 21.8 遗留待办

- [ ] 工作区 200+ 文件未提交（错峰、源清理、记录等）——建议尽快分批提交
- [x] ~~CI workflow 仍生成 update.json（main.yml:178），应用内更新已下线，需删该步骤~~（2026-08-29：用户决定保留，不做）
- [x] ~~电视侧链路：插网线后复测出画时间（预期 <2s）~~（2026-08-29：用户决定不做）
- [x] ~~预解析失败房间负缓存（30% 死房间浪费预热名额，未做）~~（✅ 已实现并验证，见二十三节）

---

## 二十二、R 项重启：arm64 ELF 裁剪 Houdini 12 崩溃根因定案 + 「原位清零」新方案 ✅（2026-08-29 深夜追加）

> 承十五节遗留：「arm64 shrunk 库在 Houdini 12 下 SIGSEGV，具体结构待深挖」。
> 本节完成根因定位并落地新裁剪方案，**恢复 -119KB arm64 收益**。

### 22.1 实验矩阵（三个变体二分根因）

| 变体 | 裁剪内容 | 逍遥2 (Houdini12) | 结论 |
|---|---|---|---|
| V1（十三节旧方案） | 删 .eh_frame/.eh_frame_hdr/.note/.comment + **LOAD 合并重排** + PHDR 表重建 | ❌ SIGSEGV（十五节 + 本次复现，fault addr 恒 …e80，huya-sdk-init 线程，libhoudini 内） | 不可用 |
| V2（新增 `_elf_shrink2.py`） | **保留全部 unwind 数据与 PH**（PT_GNU_EH_FRAME/RELRO/STACK/NOTE 重算 offset），仍做布局重排 | ❌ 同 SIGSEGV | **排除 unwind 缺失假设** |
| **V3「原位清零」（定案）** | **结构零改动**：PH/SHDR/LOAD/对齐全原样，仅把 .eh_frame + .eh_frame_hdr 的文件内容**原地写 0** | ✅ 两次冷启动 0 崩溃 | **根因 = 布局重排**（Houdini 12 对 LOAD 合并/PHDR 重排的 ELF 解析 bug），与 unwind 无关 |

工具：`_elf_shrink2.py`（V2 诊断用保留）；清零脚本见 22.2（内联于会话，逻辑 10 行）。

### 22.2 清零方案细节

- 对象（仅 arm64）：`libhycrypto.so`(.eh_frame_hdr 35580B + .eh_frame 136824B)、`libhyssl.so`(27964+112912)、`libmarsstn.so`(21308+92016)
- 操作：`f.seek(sh_offset); f.write(b'\0'*sh_size)`——**文件大小不变、offset/vaddr/PH/SHDR 全原样**
- 离线校验：清零前后 dynsym(2347/1088/265 符号)、PH 表、section 表**逐项一致**（仅内容字节变化）
- unwinder 语义：arm64 读到 version=0 的非法 eh_frame_hdr → 放弃 unwind 回退（正常路径）；ARM32 的 .ARM.exidx 是必解析的紧凑表，**清零有死循环/崩风险 → v7a 不做清零，保持原版**
- APK 内收益（so 以 deflate 存储，全零区压缩率极高）：hycrypto -47KB + hyssl -40KB + marsstn -33KB ≈ **-119KB**（APK 实测 5931004 → 5809345）
- 对比 V1：文件级 -430KB，但 **APK 内 deflate 后收益仅比清零多 ~150KB**，却以 Houdini 12 必崩为代价——清零方案完胜

### 22.3 全矩阵回归（arm64 清零版）

| 环境 | 转译层 | 结果 |
|---|---|---|
| 逍遥2 (Android 12) | Houdini 12（原必崩） | ✅ 两次冷启动 0 SIGSEGV，READY/SDK init/预解析 streams=8 全正常 |
| 雷电 (Android 14) | x86_64 转译 | ✅ 0 崩溃，READY + 预解析全成功 |
| 红米真机 (Android 14) | **arm64 原生 unwinder** | ✅ 0 崩溃（读全零 eh_frame 的关键验证），READY + SDK init + 预解析全正常 |
| 创维电视 | v7a 原生 | 不适用（v7a 未改动） |

### 22.4 定案状态更新（覆盖 17.1 SO 总表）

| SO | arm64 | v7a |
|---|---|---|
| libhycrypto.so | 1723360（原版结构，.eh_frame* 内容清零） | 1181868（原版） |
| libhyssl.so | 846280（同上） | 534392（原版） |
| libmarsstn.so | 1702472（同上） | 874596（原版） |

- 产物快照：`_shrink2_out/*.zerofill`；原版备份：`_shrink2_out/orig_backup/`
- ⚠️ **升级 SDK / 换 so 后重做清零**：重新解包 aar → 对新 so 执行同款清零（脚本逻辑见本节）
- ⚠️ **勿再使用 `_elf_shrink.py`（V1 布局重排）处理任何会被 Houdini 12 转译的 arm64 库**

### 22.5 方法沉淀

1. **二分定位要看「变量是否真正被隔离」**：V2 保留了 unwind 数据但**没保留布局**——它崩了才证明根因在布局而非 unwind；若只测「加回 unwind」的版本会得出错误结论
2. **deflate 收益与文件收益是两回事**：SO 在 APK 里是压缩存储的，删数据（要移动结构）与清零数据（结构不动）的 APK 收益差距远小于文件级差距
3. **模拟器转译器（Houdini）对 ELF 布局重排敏感**：任何 so 后处理方案优先选择「结构零改动」路线

---

## 二十三、预解析失败房间负缓存（SP 持久化）✅（2026-08-29 深夜追加）

> 承二十一.8 待办：实测源列表 ~40% 虎牙房间 code=1（未开播/下播），每轮预热浪费 1.5s/个。

### 23.1 实现（HuyaSDKParser）

- 内存表：`sPreloadFailAt`（roomId → 最近失败时间戳），阈值 `PRELOAD_FAIL_RETRY_MS = 30 分钟`
- **三个生效点**：
  1. `PRELOAD_RUNNABLE` 候选选择：负缓存内的房间 `continue` 跳过
  2. `preloadRooms()` 入队：负缓存内的房间不入队（不占名额）
  3. `parseFull` 回调：失败 → 记入；成功 → 移除（死房间重新开播可自动恢复预热）
- **SP 持久化**（`huya_preload_fail`）：init 时恢复未过期记录（过期即清理），失败/成功同步写 SP——冷启动不再重试已知死房间
- 用户点频道实时解析**不受任何影响**（不走预热队列）

### 23.2 验证（雷电，arm64 2.0.179）

| 轮次 | 预热结果 |
|---|---|
| 第一次启动 | 30 成功 / **23 失败**（全部记入负缓存，日志措辞「30 分钟内跳过预热，点频道时仍实时解析」） |
| force-stop 重启 | 🔄 **恢复 22 个死房间负缓存** → 预热 **28 成功 / 0 失败**（死房间全部跳过） |

收益：每轮预热省 ~35 秒后台预算 + 死房间请求流量；SDK/源无副作用。

---

## 二十四、v7a 三库缩减：CANTUNWIND 方案 ✅（2026-08-29 深夜追加）

> 承二十二节：arm64 清零落地后，用户要求解决 v7a 三库（hycrypto/hyssl/marsstn，AAR 内 marsstn）缩减。

### 24.1 方案演进（三个变体）

| 变体 | 做法 | 验证结果 |
|---|---|---|
| V1 布局重排（`_elf_shrink.py`） | 删 .ARM.exidx/.comment/.note + LOAD 重排，文件级 -70.7KB | ❌ **逍遥1 (Houdini 4.x) SIGSEGV**（huya-sdk-init 线程，libhoudini 内）——**十三节"v7a 裁剪版已验证"结论被推翻**，Houdini 4.x 与 12 一样对重排 ELF 敏感 |
| 原位清零 exidx | .ARM.exidx 全写 0 | ❌ 语义不可行：ARM32 unwinder 必解析 exidx 紧凑表，prel31=0 指向自身，死循环/崩风险 |
| **CANTUNWIND（定案）** | **结构零改动**：.ARM.exidx 每条 entry 的 data 字节写 `EXIDX_CANTUNWIND(0x1)`，prel31 func 原样保留（表序合法） | ✅ 逍遥1 全链路通过（crash 0、READY、SDK init、源加载） |

### 24.2 CANTUNWIND 细节

- .ARM.exidx entry = 8 字节（prel31 func + data）；data=1 → 该函数"不可回溯"（异常到达即 terminate，纯观看无异常则永不触发）；表结构/排序/大小不变 → ARM32 unwinder 正常二分解析
- 三库处理：libhycrypto（2685 entry）、libhyssl（1670）、AAR 内 libmarsstn（4866）
- 文件级收益：-20700/-12512/-37516 = **-70.7KB**；APK deflate 后 **-34KB**（v7a 包 5546290 → 5511345）
- 离线校验：dynsym/DT_NEEDED 一致，全 section congruence（offset≡vaddr mod align）无异常

### 24.3 验证状态

| 环境 | 结果 |
|---|---|
| 逍遥1 (5.1.1 + Houdini 4.x 转译 v7a) | ✅ crash 0、READY/SDK init/185 频道全正常 |
| 雷电 (14 / x86_64 + Houdini 32 位转译 v7a) | ✅ crash 0、TotalTime 1779ms、READY/SDK init/185 频道全正常 |
| 创维电视 (v7a 原生, 192.168.1.7) | ✅ crash 0、TotalTime 991ms、READY/SDK init/185 频道/EncryptedStorage 全正常——**原生 ARM32 unwinder 读 CANTUNWIND exidx 最终确认通过，v7a CANTUNWIND 定案** |
| arm64 包 | 不受影响（V3 清零版，见二十二节） |

> 2026-08-29 深夜勘误：逍遥1 上一轮「原版对照包也 DEAD」为 **`ps -A` 语法误判**（Android 5.1.1 无 -A 参数），
> V1 裁剪版的崩溃是真实的（crash buffer 有 SIGSEGV 记录），CANTUNWIND 版曾被误杀一场，重验后通过。

### 24.4 ⚠️ 重大勘误与教训（覆盖十三/十四节结论）

1. **「v7a V1 裁剪版曾在逍遥1+电视验证通过」不成立**：十四节通过的是"仅修 c++_shared"的包，当时 marsstn/hycrypto/hyssl 的 shrunk 版加载后**执行路径未真正回归**（或当时逍遥1 实例状态掩盖）；本轮同规格 V1 重新生成后在逍遥1 必崩。十七节收尾时把 v7a 还原为原版是正确决策
2. **`ps -A` 在 Android 5.1.1 无效**（toybox 老版无 -A 参数）——本轮一度把存活进程误判为 DEAD、差点冤枉 CANTUNWIND 版；判断进程用 `ps | grep <pkg>` + crash buffer + 9527 日志三重验证
3. **对照实验必须先验环境**：CANTUNWIND 版"DEAD"后先装原版对照包，原版也 DEAD → 实例坏了（重启恢复）；若无对照步骤会错杀正确方案
4. Houdini（4.x 与 12）对 ELF 的容忍度：**布局重排必崩（两代转译器同病），数据内容改写（清零/CANTUNWIND）可过**——so 后处理一律走「结构零改动」路线（与 22.5 结论合并）


---

## 二十五、ProGuard 规则精简：u db 三包 + berry.module.live 改 allowshrinking ✅（2026-08-29 深夜追加）

### 25.1 量化依据（dex 解析 + mapping.txt）

- 当前 dex 总类数 6629（classes.dex 5.95MB + classes2.dex 482KB）
- mapping 统计强 keep 块：**com.huyaudbunify 338 类 + com.huyaudb 28 类**（15.5 节最大遗留强 keep 块）、berry.module.live 66 类
- **com.hycom / com.huya.nimo / com.huyaosdk / com.huyahi / com.huyall 在 dex 中均为 0 类**（死规则，无 dex 代价，规则保留）

### 25.2 规则变更（proguard-rules.pro）

1. `com.huyaudbunify.** / com.huyaudb.** / com.hycom.**`：强 keep → **allowshrinking**
   依据：D 项后 libudbauthunify.so 为 stub（sendMsg 恒返回空），登录链路事实上已废；
   15.5 节曾实测 allowshrinking 可行（当时因"登录未实测"回退，该前提已消失）
2. `com.huya.berry.module.live.**`：强 keep → **allowshrinking**
   依据：该子包仅 ServiceHelper 反射 newInstance 创建（SdkLiveService/LiveService/
   ForceLiveService 等 11 个单类 `<init>` 规则已兜底），整包强 keep 过度

### 25.3 收益

| 指标 | 前 | 后 |
|---|---|---|
| arm64 包 | 5809345 | **5702672（-104KB）** |
| v7a 包 | 5532288 | **5407684（-125KB）** |
| dex 总类数 | 6629 | **6321（-308 类）** |

> 比 15.5 单独收缩 udb 时（-60KB）翻倍：本次叠加 berry.module.live 与 u db 全链。

### 25.4 验证（雷电 arm64）

- crash 0；185 频道加载 ✅；SDK init 成功（createService 反射链完好 = 单类 <init> 规则兜底有效）✅
- 预解析 streams=8/6/3/2 全正常 ✅；普通源播放 READY ✅
- 码率构建 berry.module.Player 不在 module.live 子包，不受影响（init 链佐证）

### 25.5 dex 拆分说明

- splits.abi 双包的 dex 本就各自独立，无跨包共享空间；
- classes2.dex（482KB）为 R8 自动分配，无需人工干预；
- "拆分"维度无额外收益，精简收益全部来自 keep 规则（本节）。


---

## 二十六、AAR stub 深水区：ServerStartManager 替换（Round1 失败 → Round2 定案）✅（2026-08-29 深夜追加）

### 26.1 背景与量化

- AAR 全量排查结论：通用第三方 jar（fastjson/WebSocket/ormlite/数美/monitor/download 等 ~1.4MB）
  **R8 已删光 dex 类**（dex 统计均 0-5 类），exclude 零收益
- 真正占 dex 的是 ServerStartManager.startModules() 注册的 14 个服务模块（~430 类）
- 反编译确认：所有服务消费点（LiveStream/HuyaBerryData/CustomUIPresenterImpl/LiveListTask）
  均为 `getService()` + **判空跳过**（`if (s != null)`）→ 服务不注册 = 功能分支静默跳过，理论无 NPE

### 26.2 Round1（全清空）→ 失败 ❌

- 做法：从 sdkclient-release.aar 的 classes.jar 删除 ServerStartManager.class（mmkv 模式），
  App 放同名 stub（startModules 空实现）
- 结果：init 成功但**全部房间解析 30s 超时**——
  **HysignalPushModule（Ark 模块）是信令长链的 Java 侧组件**，砍掉即长链断
  （症状与 P 项 hyssl stub 失败一致；此前"信令由 marsstn 原生栈独立承担"的推断是错的）

### 26.3 Round2（保留解析链必需 4 服务）→ 定案 ✅

stub 保留：
- `HysignalPushModule`（Ark.startModule，信令 Java 侧）
- `CommonService`（gamesdk 通用服务）
- `SdkLiveService` / `LiveService`（live 数据查询链）

砍掉 10 个（登录/开播/SDK 播放器 UI/反馈——纯观看不可达）：
LoginSdkService、LoginModule、UserService、SdkCameraService、SdkLiveListService、
ModifyTitleService、EndLiveService、SdkPlayerService（含 init() 播放器初始化跳过）、
ForceLiveService、FeedBackModule

配套变更：
- 全局四大组件 keep（`-keep public class * extends Service/Activity/...`）**删除**
  ——它们是服务类未收缩的主因（AbsService 层级传递匹配）；R8 full mode 下 AAPT
  自动为 Manifest 组件生成规则，全局 keep 冗余
- FeedBackModule/HysignalPushModule 的 `<init>` keep 调整；stub 类与 4 个保留服务加 keep

### 26.4 收益

| 包 | 25 节后 | 26 节后 |
|---|---|---|
| arm64 | 5702672 | **5658090（-45KB）** |
| v7a | 5407684 | **5363089（-44KB）** |

### 26.5 验证

| 环境 | 结果 |
|---|---|
| 雷电 (arm64 转译) | ✅ init 成功、清负缓存后预解析 30 成功/24 失败（正常水位）、streams=8/6/3 与基线一致 |
| 逍遥1 (v7a + Houdini 4.x) | ✅ init 成功、30 成功/22 失败、crash 0 |
| 创维电视 (v7a 原生) | ⏳ 待机待补验（v7a 路径已被逍遥1 覆盖，风险低） |

### 26.6 方法沉淀

1. **「判空跳过」≠「可安全移除」**：服务的有无会改变初始化副作用（HysignalPushModule
   移除 → 长链断）。消费点判空只能证明不 NPE，不能证明功能无损——必须实测解析链
2. **二分排查服务依赖时先用负缓存无关的干净环境**：Round1 验证曾被 SP 负缓存污染
   （3 成功/0 失败的假象），清 `huya_preload_fail.xml` 后才见真值
3. **全局组件 keep 是隐形收缩杀手**：`* extends Service` 会传递匹配 AbsService 层级，
   保留全部 SDK 服务类；R8 full mode + AAPT 规则已覆盖 Manifest 组件，全局 keep 应删
4. Round1 失败时的环境噪声（雷电 toybox grep 自身 SIGSEGV、app 意外卸载）差点误导归因——
   crash buffer 里 grep 进程崩溃 ≠ 应用崩溃，先核对 Cmdline


---

## 二十七、AAR exclude：sdkplayer-impl 整包移除 ✅（2026-08-29 深夜追加）

### 27.1 量化筛选（jar 类数 vs dex 残留）

| 层级 | AAR | jar 类 | dex 残留 | 判定 |
|---|---|---|---|---|
| 已免费 | fastjson/WebSocket/ormlite/数美/monitor/download 等 ~1.4MB | — | 0-5 类 | R8 已删光，exclude 零收益 |
| **可 exclude** | **sdkplayer-impl-release（488KB）** | 99 | **67** | 引用者仅 HuyaBerryData（接口判空）→ **本轮移除** |
| 不可动 | common-user（77 残留）、sdklive（36，Ark 事件类被 LiveStream 引用）、hyhttpdns/networkmars（gamesdk 核心）、jssdk/channelinfo（可达） | — | — | 引用链真实存在，保留 |
| 死规则清理 | endlive/forcelive/sdkcamera/modifytitle/sdklivelist 的 `<init>` keep | — | 各 1 | Round2 已不注册，规则删除后残留归零 |

### 27.2 变更

- `build.gradle` excludes 新增 `sdkplayer-impl-release.aar`（488KB，最大 impl）
  - 依据：Round2 stub 后 SdkPlayerService 不再注册；全引用扫描仅 HuyaBerryData
    以 `getService(ISdkPlayerService)` + 判空方式使用（接口在 sdkplayer-api，保留）
  - 附带效果：SDK 播放器浮窗/Fragment/弹幕 View 全链消失（BerryDebugChecker
    自检"播放器类 6/6 全部 ClassNotFound"）
- proguard 删除 5 条死 `<init>` keep（endlive/forcelive/sdkcamera/modifytitle/sdklivelist）

### 27.3 收益

| 包 | 26 节后 | 27 节后 |
|---|---|---|
| arm64 | 5658090 | **5532453（-125KB）** |
| v7a | 5363089 | **5237458（-126KB）** |

### 27.4 验证

| 环境 | 结果 |
|---|---|
| 雷电 (arm64) | ✅ init 成功、清负缓存后 30 成功/22 失败（正常水位）、streams=8/6/2、crash 0、播放器类 6/6 剥离 |
| 逍遥1 (v7a + Houdini 4.x) | ✅ TotalTime 395ms、crash 0 |
| 创维电视 | ⏳ 待机待补验 |

### 27.5 剩余评估

- common-user（77 类残留）：被 HuyaBerryImpl/Callback/LiveStream 直接引用（登录链
  类型字段），stub 需改 sdkclient 多类，成本高收益 77 类，暂不动
- sdklive（36 类）：Ark 事件类（FloatWinInterface.OnAutoPauseLive 等）被 LiveStream
  发送侧引用，exclude 会 NoClassDef，不可动
- 至此 AAR 层高性价比项全部收尾；剩余实现类均为真实可达引用


---

## 二十八、AAR 层最终收尾：login-sdk / noblegift 排除 + 死 keep 清理 ✅（2026-08-29 深夜追加）

### 28.1 本轮筛选（jar 类数 vs dex 残留 vs 引用扫描）

| AAR | jar 类 | dex 残留 | 引用扫描 | 判定 |
|---|---|---|---|---|
| login-sdk-release | 6 | 5 | LoginSdkService 唯一引用者 = 已 stub 的 ServerStartManager | **排除** |
| base-noblegift-release | 15 | 12 | **零外部引用**（12 类残留是 JCE keep 规则副作用） | **排除** |
| common-user-release | 104 | **实际仅 5 类**（前次统计为前缀误匹配） | UserProperties/UserApi（被 SdkLiveService 调）+ UserService（死 keep 保着） | 删死 keep 后 api 4 类必留，impl 删光 → **保留 aar（已免费）** |
| common-feedback | 37 | 11 | 被 base-login 的 LoginReport 引用（LoginModule 已不注册 → 链路不可达） | 保留（exclude 有 NoClassDef 风险，收益 11 类） |
| webview(jssdk)/channelinfo | 42/34 | — | 被 LiveStream 真实引用 | **不可动** |

### 28.2 变更

- proguard 删除 3 条死 keep：LoginSdkService / LoginModule / UserService `<init>`（Round2 后无注册者）
- build.gradle excludes 新增：login-sdk-release、base-noblegift-release

### 28.3 收益与验证

| 包 | 27 节后 | 28 节后 |
|---|---|---|
| arm64 | 5532453 | **5504448（-28KB）** |
| v7a | 5237458 | **5209452（-28KB）** |

雷电 (arm64)：✅ init 成功、清负缓存后 30 成功/22 失败（正常水位）、streams=8/6、crash 0。

### 28.4 AAR 层终局判定

- 可 exclude/stub 的已全部处理：webview + 开播链 8 个（08-28）、sdkplayer-impl（27 节）、
  login-sdk + noblegift（本节）、ServerStartManager 类替换（26 节）
- 剩余全部为真实可达引用：gamesdk/sdkclient 网络与数据核心、user/api 数据类、
  feedback（不可达链引用防 NoClassDef）、hyns 系网络栈
- **AAR 层无剩余可动项**


---

## 二十九、内置源3（本地 assets）整体移除 + apk_check 清理 ✅（2026-08-29 深夜追加）

### 29.1 过程实录（含两个关键发现）

1. **gzip 化遇 AAPT2 反向操作**：assets 里放 live_source_3.txt.gz 后，APK 内出现的是
   **解压后的 live_source_3.txt（772KB）**——AAPT2 对 `.gz` 扩展名会**自动解压并去掉后缀**
   打包，预压缩完全失效。对照实验证实（拿走 .gz 后 merge 输出无 txt）。
   解法：改用 `.gzt` 扩展名（AAPT2 按普通文件原样打包）+ 代码 GZIPInputStream 读取。
   复盘：原 txt 的 compress_size 147995 其实是 aapt2 已 deflate（19%），gzip level9
   142KB，**gzip 化真实收益仅 ~6KB** 而非预估 118KB。
2. **clean 构建仍出 txt 的真凶**：`apk_check/`（历史解包调试产物，含完整解包 APK）
   ——已按用户指示删除。

### 29.2 内置源3 移除（用户决定：不再需要）

- 删除 `assets/live_source_3.txt.gzt`
- **UrlConfig**：删 LIVE_URL_3_LOCAL_ASSET / LIVE_URL_3_RAW / LIVE_URL_3 三字段 + 日志 L3 段
- **LiveSourceLoader**：删 `asset://` 加载分支 + loadFromAssets 方法（gzip/.gzt 逻辑随之移除）
- **SourceManager**：删 src3 条目构建 / BuiltinSpec / isBuiltinUrl 判定；
  新增 SP 历史条目清理（`BUILTIN_NAME_LIVE_3` 常量保留用于识别并移除用户设备上
  残留的「内置源3 (本地666)」条目，切源界面不再显示）
- **SubscriptionAdapter**：删 LIVE_URL_3 保护判定

### 29.3 收益

| 包 | 28 节后 | 29 节后 |
|---|---|---|
| arm64 | 5504448 | **5355699（-141KB）** |
| v7a | 5209452 | **5070715（-131KB）** |

### 29.4 验证（雷电 arm64）

- crash 0；TotalTime 250ms
- 源1（gitee dfdvfaf）加载成功 185 频道 ✅
- URL 配置解密日志无 L3 段 ✅
- 预解析 30 成功 / 20 失败（正常水位）、streams=8/6 ✅

### 29.5 当前源体系（最终）

| 源 | 地址 | 频道 |
|---|---|---|
| 源1（默认） | gitee qf_1111/iptv dfdvfaf.m3u | 185 |
| 源2（备用） | gitee qf_1111/iptv playlist.m3u | 125 |
| ~~源3（本地 assets）~~ | **已删除** | — |


---

## 三十、废弃/无效内容大扫除 ✅（2026-08-29 深夜追加）

### 30.1 清理清单

| 类别 | 内容 | 动作 |
|---|---|---|
| 调试残留 | TVPlayerManager 9 条 `[DEBUG-doPlayNormal/1/2/3]` 红色调试日志（播放器 prepare/play 状态探针） | 删除 |
| 死 keep 规则 | proguard：`com.hycom/nimo/huyaosdk/huyahi/huyall`（dex 0 类的死包）、`LoginSdkService/LoginModule/UserService` 的 `<init>`（Round2 后无注册者） | 删除 |
| 无效 dontwarn | `com.huyaosdk/huyahi/huyall/huyaudb/umeng.socialize/de.greenrobot.event`（包不存在） | 删除 |
| build.gradle | fileTree excludes 精简重写：19 个引用已物理删除文件的无效条目移除（文件不存在时 exclude 无效果），保留 3 个有效条目（sdkplayer-impl/login-sdk/base-noblegift），大段历史注释精简（详细依据保留在开发记录 11~28 节） | 重写 |

### 30.2 排查确认无需处理的

- **零引用类**：com.tv.live 下全量扫描（含 Manifest/gradle/proguard 引用面）= **0 个孤儿类**
- **AppCacheInspector**：仍在用（清理 tv_live_update_*.apk 残留，应用内更新下线后的兜底，保留）
- **assets/ark.config**：Ark 模块系统配置（HysignalPushModule 仍在用），保留

### 30.3 验证（雷电 arm64）

crash 0、init 成功、READY、源1 185 频道、预解析正常。体积不变（本轮清理为代码卫生，无体积收益预期）。

### 30.4 当前最终状态

| 包 | 大小 |
|---|---|
| arm64-v8a | **5.36MB** |
| armeabi-v7a | **5.06MB** |

（自 08-25 的 24MB 累计 **-79%**）


---

## 三十一、全工程注释剥离 + 注释存档 ✅（2026-08-29 深夜追加）

### 31.1 操作

- 对 86 个文件执行注释剥离：`app/src/main/java/**/*.java`、`cpp/*.cpp/.c/.h`、
  `app/build.gradle`、`app/proguard-rules.pro`、`AndroidManifest.xml`
- 剥离方式：状态机剥离器（保护字符串/字符字面量中的 `//` 与 URL），
  `//` 与 `/* */` 全删，proguard 的 `#` 行注释全删，连续空行压缩
- 构建验证：剥离后 assembleRelease BUILD SUCCESSFUL（arm64 5.36MB / v7a 5.06MB 不变）

### 31.2 注释存档

- **全部被剥离的注释（5262 行 / 86 个文件）已存档至
  [开发记录_注释存档_2026-08-29.md](开发记录_注释存档_2026-08-29.md)**，
  按文件分组，供以后调用查看历史决策依据
- 存档来源 = git diff 被删行（HEAD 版本积累的注释主体）
- 例外：26~29 节会话期间新增又被剥离的少量注释（ServerStartManager stub 说明、
  .gzt 扩展名说明、AAR 排除清单精简版说明）不在 git diff 中，
  其内容已由开发记录 26~29 节正文完整覆盖

### 31.3 提示

- 后续如需恢复某条注释的上下文，先查本存档，再对照开发记录对应章节
- 新代码请遵循「注释即文档」最小化原则：只写约束与坑，不写流水账
  （历史决策一律进开发记录）


---

## 三十二、补记：APK 占比分析 + 播放器（media3）精简边界结论 ✅（2026-08-29 深夜补档）

> 本节补录两次分析型会话的结论（此前未入档）。

### 32.1 APK 占比全景（arm64 release，注释剥离后实测）

| 类别 | 压缩后 | 占比 | 状态 |
|---|---|---|---|
| dex（代码） | ~2.59MB | 47.8% | 6182→更少（30 节清理后），R8 full mode |
| lib/so | ~1.69MB | 31.3% | 四库（marsstn/hycrypto/c++_shared/hyssl）占 99%，清零/CANTUNWIND 完成 |
| res/ | ~562KB | 10.4% | shrinkResources 已开，最大单文件 45KB，无膨胀 |
| assets | ~136KB（29 节后） | ~2.5% | 内置源3 已删，剩 cacert.pem（不可动）+ baseline.prof |
| resources.arsc | ~219KB | 4.1% | 资源表本体 |
| META-INF | ~66KB | 1.2% | v1 签名必须保留（Android 5.x 兼容） |

结论：so/dex/res 三大头全部到各自边界；无集中可省项。

### 32.2 播放器（media3）精简边界结论

- 依赖：media3-exoplayer 1.7.1 + media3-ui + media3-exoplayer-hls（完整库数千类）
- **dex 实际仅剩 14 类**：ExoPlayer + HlsMediaSource$Factory + PlayerView 及其
  UI 子组件链（AspectRatioFrameLayout/SubtitleView/PlayerControlView/TrackSelectionView/
  SubtitleView/Web/Canvas 输出等）+ 视频表面类
- 已被 R8 删除：全部 Extractor（MP4/WebM/FLV/MP3…，代码直用 HlsMediaSource.Factory
  不走 DefaultMediaSourceFactory）、全部非 MediaCodec Renderer、未引入的
  dash/rtsp/smoothstreaming 模块
- **唯一理论可省点**：移除 media3-ui 改裸 SurfaceView（省 9 个 UI 类 ~30-50KB）——
  代价是 PlayerView 的 Surface 生命周期/宽高比/黑屏重试链全部手写重造，回归风险
  远超收益，**明确不做**
- 结论：播放器层已到理论边界，无精简空间
