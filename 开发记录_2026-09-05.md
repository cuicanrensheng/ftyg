# 开发记录 2026-09-05

> 本日主题：**继续减体积：c++_shared 清零/CANTUNWIND + 自编 BoringSSL 真替换 hycrypto/hyssl**
> 最终：release arm64 **5.28MB** / v7a **4.81MB**（今日 -78KB / -252KB）
> 前置教训：会话开始时未读 08-29 记录，重复提出已被 D/H/N/O 项覆盖的 stub 方案——**动手前先读完所有开发记录**

---

## 一、会话开始时的弯路（记录在案，勿重蹈）

1. 重新提出「① HuyaAuthCore 类 stub ② NSDT stub ③ NativeBridge stub」——实际 08-28/29 的 D/H/N/O 项已用更彻底方式完成（C++ 桩 so / 删 aar + Java stub）
2. 向 huyaudbunify aar 打了 HuyaAuthCore.class 补丁 → 与已验证基线产生无意义偏离 → **已还原**（从 git HEAD 提取原始 class 回填）
3. 「仅删 libudbauthunify.so」的二分实验 A 未编译即作废

**教训**：`开发记录_总结_2026-08-29.md` 是唯一事实基线（内含防误导清单）；任何新方案先对照其「已推翻结论表」。

---

## 二、Phase 1：c++_shared 清零/CANTUNWIND ✅（-33KB）

08-29 十二/二十四节的"结构零改动"方案只处理了 hycrypto/hyssl/marsstn 三库，**libc++_shared 是漏网项**：

| 库 | 处理 | 细节 |
|----|------|------|
| arm64 libc++_shared（1058904B） | 原位清零 `.eh_frame`(77592B) + `.eh_frame_hdr`(18212B) | V3 方案（22.2 节），PH/SHDR/dynsym 全不动，仅内容写零 |
| v7a libc++_shared（657000B） | `.ARM.exidx` 1850 entry 中 1786 个 data 字写 CANTUNWIND(0x1) | 24 节方案 |

- 离线校验：SHDR 全表一致、ELF 头一致、差异字节全部落在目标区（arm64 差异 57441B；v7a 7144B=1786×4）
- 备份：`_shrink2_out/cxx_orig_backup/`
- 收益：arm64 **-29.8KB**、v7a **-3.2KB**
- 三环境回归（逍遥2 arm64/H12、逍遥1 v7a/H4、逍遥0 9）：SDK init ✅、预解析 24/25 条、crash 0 ✅

---

## 三、Phase 2：自编 BoringSSL 真替换 hycrypto/hyssl ✅（-45KB/-219KB）

### 3.1 可行性依据（ELF 符号面新鲜审计）

- marsstn 对 hycrypto+hyssl 的直接导入仅 **30/32 个符号**（arm64 24 SSL+6 X509/ERR；v7a 多 `SSL_pending`/`SSL_want`——**32/64 位契约差异再次出现，必须分 ABI 审计**）
- marsstn 二进制内无 dlsym 嫌疑字符串（SSL_/X509_/TLS_ 前缀全在导入表内）→ 静态契约完备
- hyssl→hycrypto 的 326 导入是原 hyssl 内部实现细节，双库一起替换后消失

### 3.2 构建链（BoringSSL main @ NDK 27.0.12077973）

```
cmake -GNinja -DCMAKE_TOOLCHAIN_FILE=$NDK/build/cmake/android.toolchain.cmake
      -DANDROID_ABI=<abi> -DANDROID_PLATFORM=android-21 -DBUILD_SHARED_LIBS=0
      -DCMAKE_C_FLAGS="-ffunction-sections -fdata-sections -Os -fno-asynchronous-unwind-tables -fno-unwind-tables"
      -DCMAKE_CXX_FLAGS="同上 + -fno-exceptions -fno-rtti"
→ libcrypto.a + libssl.a（静态）
```

链接（关键参数，缺一不可）：
```
clang --target=<triple> -shared \
  -Wl,--whole-archive libcrypto.a -Wl,--no-whole-archive \
  -Wl,--gc-sections -Wl,--version-script=.crypto_ver.txt \
  -Wl,-soname,libhycrypto.so -O2 -o libhycrypto.so cxx_shim.o -lm
libhyssl.so = libssl.a + libhycrypto.so + cxx_shim.o（+ DT_NEEDED libhycrypto.so）
```

### 3.3 踩坑实录（每一条都真实消耗了时间）

| 坑 | 现象 | 根因/解法 |
|----|------|----------|
| ① toolchain 未生效 | libcrypto.a 里是 x86_64 对象（avx2-x86_64-linux.S.o 可真编译） | ANDROID_ABI 显示 UNINITIALIZED；须显式传 `ANDROID_NDK` 重配。x86_64 .S 成员本身是带预处理守卫的空壳（无害） |
| ② 静态库成员不被拉入 | .so 只有 5KB、2 个 dynsym | 链接 .a 必须配 `--whole-archive`（无未定义引用时成员不提取） |
| ③ 版本脚本"不导出" | whole-archive 拉全了（12.5MB）但 dynsym 仍空 | BoringSSL CMake 给所有目标加了 `-fvisibility=hidden`（579 处）——hidden 符号不进 dynsym、不是 gc 根。解法：**sed build.ninja 把 hidden 改 default**（每次 cmake 重新生成后都要重打） |
| ④ gc 全回收 | 导出符号没被当 gc 根 | 与 ③ 同根因，非 lld bug |
| ⑤ 12.5MB 里全是 debug 段 | --gc-sections 对非 alloc 段无效 | `llvm-strip --strip-unneeded` 收尾 |
| ⑥ hyssl 大量 C++ UND | 44 个 std::__ndk1/bssl 符号悬空 | `-static-libstdc++` 被 NDK 驱动忽略（unused warning）；C++ 模板（`__sort`）在 **libc++_static.a**、`std::terminate/exception` 在 **libc++abi.a**（r27 分开归档）→ 最终 `cxx_shim.cc` + `-lc++_static -lc++abi` |
| ⑦ 设备实测 "failed to link libhycrypto.so" | 首版部署逍遥2 全挂 | 我们的 hycrypto 引用 `_ZdlPv`/`__libcpp_verbose_abort`（bcm.cc 是 C++）但 DT_NEEDED 没挂 libc++_shared；且打包的 libc++_shared（老 NDK）没有 `__libcpp_verbose_abort` 导出。**解法：cxx_shim + -fno-exceptions 后 C++ 依赖归零，库完全自包含（只依赖 libc/libm）** |
| ⑧ FORTIFY 误判 | `__memcpy_chk/__memset_chk` 被当 hycrypto 需导出 → lld "symbol not defined" | bionic libc 自带；is_libc 判定补 `_chk` 后缀 |
| ⑨ marsstn v7a 多 2 符号 | 首版 v7a hyssl 缺 `SSL_pending/SSL_want` | **版本脚本必须按 ABI 的 marsstn UND 逐一生成**（复刻 08-28 记录 11.17.1 教训） |

**cxx_shim.cc**（几十字节替代整个 libc++）：
```cpp
namespace std { inline namespace __ndk1 {
__attribute__((noreturn, cold)) void __libcpp_verbose_abort(const char *, ...) { std::abort(); }
} }
void operator delete(void *p) noexcept { std::free(p); }
```

**迭代收敛**（`_boringssl/link_iter.py`）：链 hyssl → 收集非 libc 的残留 UND → 若 libcrypto.a 有定义则追加进 crypto_ver.txt → 重链，直至 hyssl 的 UND ⊆ hycrypto 导出 ∪ bionic libc。终态（正确解析器复核）：

| | hycrypto 导出 | hyssl 导出 | hyssl UND 未解析 | hycrypto UND |
|---|---|---|---|---|
| arm64 | 387 | 24 | 4（全是 libc：gettimeofday/memchr/memcmp/time） | 62（bionic libc + OPENSSL_memory_* 弱钩子×3） |
| v7a | 387 | 26 | 4（同上） | 61（同上） |

> OPENSSL_memory_* 是 BoringSSL 的可覆盖弱钩子，弱未定义在 bionic 上解析为 0，库内部有判空，合法。

### 3.4 体积与回归

| | 原版（清零/CANTUNWIND 后） | 自编版 | raw 降幅 |
|---|---|---|---|
| arm64 hycrypto+hyssl | 2,569,640 B | 1,526,672 B | **-1,042,968** |
| v7a 同 | 1,716,260 B | 903,900 B | **-812,360** |

APK：arm64 5.33→**5.28MB**、v7a 5.06→**4.81MB**（so 压缩后 arm64 767→761KB 不明显，v7a -227KB 明显；v7a 原库 32 位代码密度低压缩差，替换收益大）

三环境回归 **全过**：SDK init ✅ / 预解析 30 条 streams=2/3（**TLS 数据链路真实打通**）/ 0 错误 0 crash。
（逍遥1 的「KeyGenerator AES not found」为 08-29 九.4 已文档化的转译环境非阻塞项）

### 3.5 备份与重建

- 被替换前的原版（清零/CANTUNWIND 版）：`_boringssl/orig_backup/`
- 构建树：`_boringssl/build_arm64|build_v7a/`、链接脚本 `crypto_ver*.txt`/`ssl_ver*.txt`、迭代器 `link_iter.py`、垫片 `cxx_shim.cc`
- **升级 SDK/换 so 后**：重跑 link_iter.py 流程（版本脚本按新 marsstn 的 UND 重新生成，ABI 分别审计）

---

## 四、累计体积（release，08-25 基线 24MB）

| 包 | 08-29 终态 | 今日 | 降幅 |
|----|-----------|------|------|
| arm64-v8a | 5.36 MB | **5.28 MB** | -78KB |
| armeabi-v7a | 5.06 MB | **4.81 MB** | **-252KB** |
| 累计 vs 24MB | | | **-78% / -80%** |

## 五、-Oz / LTO 实验定案：负收益，勿再试 ⚠️（2026-09-06 追加）

| 变体 | arm64 hycrypto | v7a hycrypto | 结论 |
|------|---------------|--------------|------|
| 基线（-Os，已验证） | 1,317,168 | 759,928 | ✅ 当前部署 |
| -Oz + LTO | 1,329,400（**+12KB**） | 800,104（**+40KB**） | ❌ ThinLTO 跨模块内联把 BoringSSL 包装层代码越并越大 |
| -Oz 单独 | 1,317,168（±0） | 759,928（±0） | ❌ 与 -Os 字节级同尺寸，零收益 |

- 已恢复为基线版本（`_boringssl/out_*_preLTO/` 备份；jniLibs cmp 一致），APK 未受影响
- 结论：自编 BoringSSL 的体积已到当前编译参数的下限；**后续勿再投入 LTO/-Oz 方向**

## 六、待办

- [ ] 真机（5555 红米，Android 14，arm64 原生）回归本次两阶段改动（真机 unwinder/原生 linker 与转译环境行为不同，务必补验）
- [ ] 创维电视（v7a 原生）回归
- [ ] 崩溃监控观察期：自编 BoringSSL 在长会话/切后台/弱网下的稳定性
- [ ] 若后续把 arm64 hyssl 里的 `-fno-unwind-tables` 想撤回（如需 C++ 异常回溯），需同步撤 cxx_shim 方案评估
