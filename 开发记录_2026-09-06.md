# 开发记录 2026-09-06

> 本日主题：**真机（红米 25102RKBEC / Android 14 / arm64 原生）首验 + 真机暴露的 registerReceiver 修复**

---

## 一、真机首验（昨日减体积改动）

设备：`emulator-5554` = 红米 25102RKBEC（Android 14，arm64 **原生**，非转译）——此前所有回归都在转译模拟器上，这是自编 BoringSSL / c++_shared 清零改动首次跑在原生 linker + 原生 unwinder 上。

| 项 | 结果 |
|----|------|
| SDK init | ✅ |
| 播放就绪 STATE_READY | ✅ 2 次（真实出画） |
| 预解析 | 30/27 条成功 |
| dlopen / UnsatisfiedLink / NoClassDef | 0 |
| crash buffer | 0 |

→ **真机回归通过**，待办清单中的真机项销项（创维电视仍待验）。

---

## 二、真机暴露的 bug：RECEIVER_NOT_EXPORTED 修复从未生效（已修复）

### 2.1 现象（仅 Android 14 真机出现）

```
W/System.err: java.lang.SecurityException: com.tv.live: One of RECEIVER_EXPORTED
  or RECEIVER_NOT_EXPORTED should be specified when a receiver isn't being
  registered exclusively for system broadcasts
  at com.tv.live.MyApplication.registerReceiver(SourceFile:2)
  at com.tencent.mars.comm.Alarm.start(SourceFile:124)
  at com.tencent.mars.BaseEvent.onCreate(Native Method)
  at com.huya.hysignal.core.HySignalClient.init(...)
  at com.huya.mtp.hyns.MtpMarsTransporter.initHal(...)
```

mars 告警定时器（Alarm）注册 receiver 失败 → mars 重连/定时链路在 Android 14 真机上降级。

### 2.2 根因

08-25 的修复用了**不存在的反射方法**：

```java
IntentFilter.class.getMethod("setReceiverFlags", int.class)  // NoSuchMethodException
```

该 API 根本不存在（导出标志是 registerReceiver 的 **flags 参数**，不是 IntentFilter 的属性），且异常被 `catch (Throwable ignore)` 静默吞掉——**修复从未生效**。模拟器（Android 9/12/5.1.1）没有 targetSdk 34 的导出检查，所以一直没暴露；Android 14 真机 + `mars Alarm.start` 的 2 参 registerReceiver 首次触发检查。

### 2.3 修复（MyApplication.java）

删除反射方案，改用正规 flags 透传：

- 4 个 registerReceiver 重载统一走 `registerReceiverCompat(receiver, filter, permission, scheduler, flags)`
- API 33+：调用方未显式声明导出标志时默认补 `Context.RECEIVER_NOT_EXPORTED`（系统广播与自有广播均可送达）；已显式声明则尊重原值
- 有 permission/scheduler 时走 5 参 `super.registerReceiver(receiver, filter, permission, scheduler, flags)`

### 2.4 验证

修复包装机后：RECEIVER 导出异常 **0 条**，SDK init / 播放 / 预解析全正常（见上表）。

---

## 三、真机日志其余项甄别（均无害）

| 日志 | 定性 |
|------|------|
| `recursive attempt to load library libmarsstn.so` | bionic 对同线程二次 dlopen 的提示，1 条，无害 |
| `LogToES.writeLogToFileReal IOException: No such file or directory` | 虎牙 SDK 内部 es 日志文件落盘失败（目录不存在，被 SDK 捕获仅打印），仅影响 SDK 内部日志文件，功能无损。可选优化：HuyaCacheGovernor 预建该目录 |
| CrashHandler「全局崩溃捕获器已初始化」出现 2 次（08:13:58 / 08:14:00） | 设计行为：App 首次注册 + SDK init 成功后「夺回」重新注册（总结 9.3） |

## 四、日志工具显示重复问题（工具侧，非应用 bug）→ 已修复

**真机手动压测（用户 1 分钟内 3 次冷启动）**：全部完整走通（STATE_READY 1080p 出画 ×3），无重复刷屏、无 SecurityException、迭代器噪音 0 条、负缓存正常恢复、"记住上次频道"正常、Cache API 503 被重试+兜底扛住——当日全部修复项在压测下稳定。

现象：TV Live 日志工具 Pro 中 ExceptionReporter/CrashHandler 等行成对重复（同一秒的启动日志重复 ~10 次）。

根因（工具源码 `D:\ASDF\TV Live\日志工具-Pro\app\core.js` 审计）：
1. **9527 轮询游标按缓冲长度判断新旧**——应用缓冲轮转/重启导致 `all.length` 回落时，整批重新导入
2. **logcat 通道重连时 `logcat -v time` 全量重放**已有缓冲
3. 双通道（logcat 直抓 + 9527 轮询）各送一份同一条日志，无任何去重

修复（core.js）：`_push` 入口加内容指纹去重——键 = `秒级ts(tag前归一化去掉日期与毫秒)+tag+msg`，`seenKeys` Map 容量 20000（超限淘汰一半最旧），`clear()` 同步清空。单元验证：logcat 版 + 9527 版 + 同秒重放三份同一条 → 收敛 1 条；不同消息正常收录。

打包：关闭运行中的工具 → `npm run build`（electron-builder）→ `release/win-unpacked/` 已更新（08:43）→ 已重新启动。

---

## 六、电视日志全量分析 + 业务失败日志降噪 ✅

电视（192.168.62.247）9527 全量日志分析（1791 条）：
- E 级 100 条中 44 条为**死房间业务失败重复打点**：每个 code=1 房间产生 `[HUYA_BIZ_FAIL]`（LogBridge）+ `[MASKED_BIZ]`（LogCollector 重复入队被 filterSensitive 脱敏）两条
- 「严重卡顿 2051/3384/11806ms」全部位于**冷启动首帧阶段**（含全新安装首次加载），非播放中途卡顿
- `检测到 Root 环境（仅记录）` ×7：创维电视本身 root，仅记录策略

修复（ExceptionReporter.reportHuyaBusinessFailure）：删除冗余 `LogCollector.error` 重复入队（LogBridge.e 内部已同步进采集器，重复条目还被脱敏成 `[MASKED_BIZ]`）。装机验证：`MASKED_BIZ` 22 → **0**，每死房间收敛为 1 条 `HUYA_BIZ_FAIL`，SDK init / 播放正常。全设备矩阵（模拟器×3 / 真机 / 电视）在此版本下回归完毕。

## 七、低码率源调研定案：央视网官方流不可用作低码率替代 ✅

背景：转译模拟器卡顿（1080p 软解），用户源（移动 IPTV 中继→央视网内容）为单码率 4.3Mbps，无档位可降。调研从央视网官方取低码率流的可行性：

1. **重定向链追踪**（江西卫视/CCTV1）：gitee m3u → 302 → **中国移动 IPTV CDN**（`2409:8087::/32`，IPv6）→ 单码率媒体列表。无 master、无档位参数，链路终点不是央视网 CDN
2. **央视网官方 API**（`vdn.live.cntv.cn/api2/liveHtml5.do?channel=pa://cctv_p2p_hdcctv1`）：
   - `video_protect=3`，视频地址为**加密引用**（hls3 需用 client_sid 在播放器 JS 内解密，token 有时效）
   - **无标清档**：`pa://cctv_p2p_cctv1` → `ack=no，该视频不存在`（web 端仅高清一档）
   - 音频流（hls6）倒是无保护 m3u8，但只有声音
3. 结论：**低码率替代流的来源不存在**——IPTV CDN 无档位、央视网有保护且仅高清。可行路径回落到：源内已有 1.6~1.7Mbps 频道（CCTV3综艺/CCTV6电影/北京/广东/江苏卫视，实测）+ 播放期暂停预解析（方案 2 待定）
4. 实验脚本：`_cctv_tiers.py`（保留）

## 五、待办

- [x] ~~创维电视（v7a 原生）回归本次改动~~（2026-09-06 ✅：192.168.62.247，创维 3T223_P31 / Android 9 / v7a 原生，卸载重装 2.0.179 后 SDK init ✅、STATE_READY 出画 ×6、预解析正常、上报五通道关闭 ✅、EncryptedStorage ✅、一起看 460 频道 ✅、0 dlopen/0 crash/0 Houdini 异常。注意：电视 adb 随待机关闭，连接前需开机；adb forward 19550 → 9527）
- [ ] （可选）HuyaCacheGovernor 预建 LogToES 目录，消除 SDK 内部 IOException 打印
- [ ] 工作区改动分批提交
