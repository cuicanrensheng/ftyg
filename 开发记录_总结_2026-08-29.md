# 开发总结（截至 2026-08-29）

> 本文件基于 08-25 ~ 08-29 五份开发记录，**并对照当前工作区源码/文件逐项核对后**汇总。
> 用途：作为唯一可信的「当前事实基线」，替代散落在各天记录中的过时/错误结论。
> 核对方式：`build.gradle`、`CMakeLists.txt`、`proguard-rules.pro`、`jniLibs/`、`app/libs/`（AAR 内 so 逐一解包验证）、最终 APK 体积。

---

## 一、项目当前状态（事实基线）

**应用**：虎牙 TV 直播（`com.tv.live`，versionName 2.0.179）
**ABI**：仅 `arm64-v8a` + `armeabi-v7a`（splits 配置，x86/x86_64 不打包）

### 1.1 jniLibs 部署的 so（已 ELF 裁剪）

| 库 | arm64-v8a | armeabi-v7a | 说明 |
|---|---|---|---|
| libhycrypto.so | **1511KB**（原 1683KB） | **1134KB**（原 1154KB） | 真实 BoringSSL，裁剪后 |
| libhyssl.so | **690KB**（原 826KB） | **510KB**（原 522KB） | 真实 BoringSSL，裁剪后 |
| libmarsxlog.so | 9KB | 8KB | 未裁剪（xlog 链路，极小） |
| libstlport_shared.so | 2KB | 1KB | 未裁剪（极小） |

### 1.2 AAR 内 so 状态

| AAR | 状态 |
|---|---|
| `hyssl-0.1.11.aar` | **jni/ 已清空**（真实 BoringSSL 已提取至 jniLibs；原版 `_backup/hyssl-0.1.11.aar.orig`） |
| `hysignal-quic-1.5.113.aar` | arm64/v7a 的 **libmarsstn.so 已裁剪**（1663→1550 / 854→817KB）；**x86/x86_64 保留原版**（不打包，刻意不动） |
| `mmkv-1.0.12.aar` | libmmkv.so 为 **stub 版**（arm64 8.7KB / v7a 6KB；原版 AAR `app/libs/mmkv-1.0.12.aar.bak`） |
| `common-gamesdk-release.aar` | jni/ so 已移除 + `assets/image/smile/` 表情已剔除（原版 `_backup/common-gamesdk-release.aar.orig`） |
| `mtpencrypt-adr-1.0.2.aar` / `hydeviceid-1.5.24-np.aar` 等 | **fileTree exclude，编译期不参与打包**（文件保留在 libs/ 目录便于回滚） |

### 1.3 本地 CMake stub 库（CMakeLists.txt）

| 库 | 源码 | 替代对象 |
|---|---|---|
| tvlive_security | （自身安全模块） | - |
| udbauthunify | `udbauthunify_stub.cpp` | SDK 原 4.39MB libudbauthunify.so |
| hyquic | `hyquic_stub.cpp`（16 个 posix_quic 符号，visibility default） | SDK 原 4.09MB libhyquic.so |
| nsdt | `nsdt_stub.cpp`（8 个 JNI 方法） | SDK 原 libnsdt.so |

### 1.4 build.gradle 关键决策（当前生效）

- **excludes（排除打包）**：`**/libBugly_Native.so` + 7 个 AAR（mtpencrypt、hydeviceid、sub-gamesdk-webview 等）
- **不 exclude**（曾试过被推翻，已注释保留证据）：`libmmkv.so`、`libhycrypto.so`、`libhyquic.so`（改由 stub 替代）
- **pickFirsts**：`libudbauthunify.so`、`libhyquic.so`、`libc++_shared.so`、`libhyaudioengine.so` → 本地 stub 优先于 AAR 同名 so
- `useLegacyPackaging true`：Android 5.x 必须（否则 so 无法映射加载，实测崩）
- 加固：`releaseJiaGu`（assembleRelease + dpt-shell 加固自动重签）

### 1.5 proguard（v4 + v5 精准收缩）

- `-keep,allowshrinking class com.huya.** { *; }`（v4：删不可达类，不混淆）
- `-keep class * extends com.duowan.taf.jce.JceStruct`（179 个 JCE 结构体）
- `-keep com.duowan.taf/kiwi/networkmars.**` + `-keep,allowshrinking com.duowan.**`
- 反射构造器强 keep 11 类、JCE/Wup 协议包、`com.huya.berry.module.**`、`com.huya.mtp.**`、retrofit/gson 等

### 1.6 最终产物

| 版本 | arm64-v8a | armeabi-v7a |
|---|---|---|
| **release（当前）** | **5.37MB** | **5.14MB** |
| 加固产物（dpt） | `release/dpt/*_signed.apk`（壳开销 +5.7MB） | 同左 |

---

## 二、五天时间线（每会话核心）

### 08-25：SDK 接入 + 码率档位修复 + proguard v3.3
- 修复「码率档位递减/残留」**真实根因 = Channel 对象 mainPlayUrl 污染**（`setMainPlayUrl` 覆写 + 缓存），修复 3 处
- proguard 从 v3.1/v3.2（allowshrinking 失败）回退 v3.3 全量强 keep

### 08-26：稳定性 + 去 x86 + 创维自启
- xlog 日志链路修复、移除 x86/x86_64 打包、创维电视开机自启动、日志脱敏

### 08-27：Bugly 移除 + 虎牙一起看 5 大分组
- Bugly 上报链路关闭；「虎牙一起看」5 固定分组（电影/电视剧/动漫/综艺/一起看），**用户分组优先**排序
- `HuyaTogetherWatchGroupManager`（SDK 独立路线）+ 预解析从主线程迁至独立 HandlerThread → 启动无 ANR

### 08-28：9 分组分类 + 加固 + 减体积专项（关键日）
- HTTP 路线 623 频道 9 分组 + 综艺源纯净化（关键词过滤）
- 一起看分组为空修复（A1 推荐未命中关键词收容兜底）；默认清晰度改为第 2 档
- 应用内更新功能下线（含删 update.json）
- dpt-shell 加固替换 360（`releaseJiaGu`）+ CI 自动加固
- **减体积专项**：D 项 udb stub（-4.39MB）、H 项 hyquic stub（-4.09MB）、F 项 aar 排除、smile 剔除（-107KB）、P 项 BoringSSL 恢复、R8 v4/v5（-0.58/-0.21MB）

### 08-29：SDK 初始化修复收尾 + ELF 裁剪（最终日）
- mmkv/hycrypto **不可 exclude**（回退错误决策，改为保留 stub 版/真实库）
- nsdt stub、hydeviceid 移除、Android 5.1.1 逍遥回归
- P 项定案：真实 BoringSSL 恢复（两轮 stub 均失败）
- mtpencrypt 整体移除（Java 明文替代）
- **R 项：ELF 紧凑重写裁剪**（`_elf_shrink.py`，剥离 .eh_frame，-490KB）→ 最终 5.37/5.14MB

---

## 三、体积优化全链路（release，两 ABI）

| 阶段 | arm64-v8a | armeabi-v7a | 本步降幅（arm64） |
|---|---|---|---|
| 08-25 基线 | 24MB | ~23MB | - |
| 08-28 B 项（mmkv 剔除实验，后回退） | 10.12MB | 8.92MB | -58% |
| D 项 udb stub | 8.88MB | 8.20MB | -1.24MB |
| H 项 hyquic stub | 7.39MB | ~6.7MB | -1.49MB |
| F 项 aar 排除 | 7.37MB | - | -20KB |
| smile 剔除 | 6.47MB | 6.09MB | -107KB |
| R8 v4（com.huya allowshrinking） | 5.89MB | 5.53MB | -0.58MB |
| R8 v5（com.duowan 结构体级） | 5.68MB | 5.32MB | -0.21MB |
| **R 项 ELF 裁剪（当前）** | **5.37MB** | **5.14MB** | **-0.31MB** |

> 注：H 项后 v7a 为约数；13.4 节 smile 剔除前的 6.58MB 与 F 项后 7.37MB 之间的
> ~0.8MB 缺口对应 08-28 记录 11.22 之后未单独列体积的中间项，最终值以各节标注为准。

---

## 四、防误导清单（旧历史 vs 当前事实）⭐

> 以下旧记录结论已被验证推翻/取代，**一律以本文件为准**。

| # | 旧结论（出处） | 当前事实 | 状态 |
|---|---|---|---|
| 1 | 「单独剔除 libmmkv.so 完全安全」（08-28 11.11） | kiwi-barrage 弹幕 SDK **必经** `System.loadLibrary("mmkv")`，exclude → `HuyaBerry init failed`。**不可剔除**；当前打包的是 stub 版（8.7KB/6KB），原版 450KB 在 `.bak` | ❌已推翻（08-29） |
| 2 | 「码率问题 = R8 收缩删了 SMObject/PlayerHelper 构建代码」（08-25 proguard 注释） | 真实根因是 **Channel 对象污染**（App 侧缓存覆写），v3.3 全量 keep 下 13:50 仍复现才是铁证。与 R8 无关 | ❌错误归因（08-28 十四节纠正） |
| 3 | 「smile 表情剔除可能有风险」（08-28 11.8 与 udb 一起回退） | smile 单独验证**可安全剔除**（9527 日志 0 错误）；11.8 失败确系 udb 引起 | ✅已澄清（08-28 十三节） |
| 4 | 「libhyssl.so 可 stub（v1 空实现 / v2 转发系统 OpenSSL）」 | 两轮**均失败**：空实现 TLS 建链失败；转发方案因 Android 8.0+ **linker 命名空间隔离** dlsym 全 NULL。定案恢复真实 BoringSSL + ELF 裁剪 | ❌已推翻（08-29 十一节） |
| 5 | 「恢复保留 libmmkv.so（约 450KB）」（08-29 早前表述） | 保留的是 **stub 版**（arm64 8.7KB / v7a 6KB），450KB 是原版大小，切勿误以为打包的是原版 | ⚠️表述修正（08-29） |
| 6 | 「mtpencrypt 需 stub（风险高，暂缓）」（08-28 待办） | 改为**整体移除 AAR + Java 纯明文替代**，无需 stub（`UnsatisfiedLinkError` 是 Error 无法被 try-catch 捕获，故不可只删 so） | ✅方案变更（08-29 十二节） |
| 7 | 「libhyquic.so 直接 exclude」 | 不行（marsstn DT_NEEDED）；改用 **C++ stub**（16 个符号 + visibility default） | ❌已推翻（08-28 11.16） |
| 8 | AAR 内 x86/x86_64 的 libmarsstn.so 未裁剪 = 遗漏？ | **不是遗漏**：splits 只打 arm，x86 系不打包，刻意保留原版 | ⚠️非问题 |

---

## 五、备份与回滚地图

| 备份位置 | 内容 | 回滚目标 |
|---|---|---|
| `_ehframe_backup/` | ELF 裁剪前原版 6 so（hycrypto/hyssl/marsstn × arm64/v7a）+ `hysignal-quic-1.5.113_orig.aar` | 恢复未裁剪 so |
| `_backup/hyssl-0.1.11.aar.orig` | 含真实 BoringSSL 的 AAR 原版 | 恢复 hyssl AAR |
| `_backup/common-gamesdk-release.aar.orig` | 含 smile/jni so 的 AAR 原版 | 恢复 gamesdk AAR |
| `app/libs/mmkv-1.0.12.aar.bak` | mmkv 原版 AAR（450KB so） | 恢复真实 mmkv |
| `_backup/mmkv-1.0.12.aar.orig2` | mmkv stub 版快照（与当前一致） | 校验用 |
| `_mmkv_stub/` | mmkv stub 源码（`mmkv_stub.c` + patch 脚本） | 重建 stub |
| `_backup/` 其余 *.orig | 各阶段被改 AAR 原版 | 逐项回滚 |

---

## 六、遗留待办（截至 08-29 未完成）

1. **dpt 加固包真机验证**（加固后安装/启动/取流/反调试触发）
2. **虎牙一起看 5 分组真机（电视）回归**
3. **工作区大量未提交改动**（安全监控、反调试、SDK 上报关闭等）需统一提交整理
4. 远程 `.github/workflows`、`.gradle` 与本地对齐
5. （可选，收益极小）libmarsxlog + libstlport_shared ~11KB 不再优化

> 2026-08-29 移除：~~CI 自动生成 update.json 停用~~、~~电视插网线复测出画~~（用户决定不做，不再跟踪）

---

## 七、关键命令速查

```powershell
# 常规构建
.\gradlew assembleRelease                        # 未加固
.\gradlew releaseJiaGu                           # 编译 + dpt 加固自动重签

# ELF 裁剪（如需重跑或处理新 so）
python _elf_shrink.py <in.so> -o <out.so>

# 产物位置
app\build\outputs\apk\release\tv_live_release_{arm64-v8a,armeabi-v7a}_2.0.179.apk
app\build\outputs\apk\release\dpt\*_signed.apk   # 加固版
```

---

*核对时间：2026-08-29。本文件事实以当前工作区为准；后续任何修改若与本文件冲突，需同时更新本文件。*

---

## 八、2026-08-29 下午追加（详见开发记录_2026-08-29.md 第二十一节）

| 项 | 当前状态 |
|---|---|
| 直播源地址 | **源1 = `https://gitee.com/qf_1111/iptv/raw/master/dfdvfaf.m3u`（185 频道）**；源2 = `.../playlist.m3u`（125 频道）。旧 GitHub 源1（慢）与 iptvedqu.m3u（404）的密文/迁移逻辑已从 UrlConfig+SourceManager **彻底删除** |
| LiveSourceLoader 镜像竞速 | 保留（仅对 GitHub URL 生效；源1 为 gitee 后自动跳过，供未来自定义 GitHub 源使用）。竞速时限 5s，超时转源2，未完成镜像后台落缓存 |
| 预解析错峰 | 已装机验证（电视/雷电/逍遥×3 全过）：READY 前 0 预解析请求，READY 后立即放行；20s 兜底保留 |
| 多端出画实测 | 雷电 14：首装 3s/缓存 1.5s；逍遥 5.1.1/9/12：2~4s；创维电视 9：~7.5s（其中应用侧 ≤1s，6s 为电视链路 TTFB——PC 直连同服务器 45ms，服务器不慢） |
| ⚠️ 新环境教训 | ① 9527 端口**禁用 adb reverse**（adbd 会抢占设备端端口致空响应），只用 forward；② `首次学习证书` 是握手完成时间戳（TOFU 毫秒级），非慢因；③ 电视待机后 adb 关闭，ping 通≠adb 通 |
| 17.5 逍遥2 黑屏 | 未复现（Android 12 正常出画），建议销项 |


---

## 九、2026-08-29 深夜追加（详见开发记录_2026-08-29.md 第 22~32 节）

| 项 | 当前状态 |
|---|---|
| SO arm64 | hycrypto/hyssl/marsstn 改「原位清零」（.eh_frame 内容写零，结构零改动）——V1 布局重排在 Houdini 12 SIGSEGV 根因定案后废弃；-119KB |
| SO v7a | 三库改「CANTUNWIND」（.ARM.exidx data 置 1，结构零改动）——逍遥1+雷电+电视三环境验证；-14KB。V1 重排在 Houdini 4.x 同样崩（十三节旧结论推翻） |
| R8 精简 | huyaudbunify/huyaudb/hycom + berry.module.live 改 allowshrinking（-308 类）；全局四大组件 keep 删除（AAPT 规则已覆盖，传递匹配保留全部 SDK Service 是服务类删不掉的真因） |
| ServerStartManager | 类替换 stub（Round2 定案：保留 HysignalPush/CommonService/SdkLiveService/LiveService，砍 10 个登录/开播/播放器UI 模块）。Round1 全清空失败——HysignalPushModule 是信令长链 Java 侧组件 |
| AAR exclude | sdkplayer-impl（488KB，引用者仅接口判空）、login-sdk、base-noblegift；AAR 层无剩余可动项 |
| 内置源3 | 整体移除（assets+UrlConfig+LiveSourceLoader+SourceManager+SP 清理）；**AAPT2 会解压 .gz 扩展名并去后缀**（gzip 预压缩须避开 .gz）；apk_check 调试产物删除 |
| 大扫除 | DEBUG 调试日志 9 条、死 keep 11 条、无效 dontwarn 6 条、fileTree 无效排除 19 条全清；App 零孤儿类 |
| 注释剥离 | 86 文件全剥离（状态机保护字符串），**5262 行注释存档于 开发记录_注释存档_2026-08-29.md** |
| 播放器 | media3 dex 仅剩 14 类（R8 已到边界），明确不做 UI 层移除 |
| **最终体积** | **arm64 5.36MB / v7a 5.06MB**（08-25 基线 24MB，累计 -79%） |
| 环境新教训 | `ps -A` 在 Android 5.1.1 无效（误判进程死亡）；crash buffer 里 grep 崩溃≠应用崩溃；负缓存会污染二分验证（先清 huya_preload_fail.xml）；AGP merge assets 增量不感知删除（需 clean） |

---

## 十、2026-09-05 追加（详见 开发记录_2026-09-05.md）

| 项 | 当前状态 |
|---|---|
| Phase 1 | arm64 libc++_shared `.eh_frame/.eh_frame_hdr` 原位清零 + v7a CANTUNWIND（复用 22/24 节方案）；-33KB |
| Phase 2 | **hycrypto/hyssl 整体替换为自编 BoringSSL**（NGD27 构建，-fno-exceptions/-fno-rtti/-fno-unwind-tables + cxx_shim 免 libc++ 依赖 + 版本脚本受限导出 + gc-sections）；marsstn 契约 arm64 30/30、v7a 32/32 全满足 |
| 回归 | 逍遥2/逍遥1/逍遥0 三环境全过（SDK init ✅、预解析 30 条 streams=2/3、0 crash）；真机/电视待补验 |
| **最终体积** | **arm64 5.28MB / v7a 4.81MB**（08-25 基线 24MB，累计 -78%/-80%） |
| 备份 | 被替换的原版 hycrypto/hyssl 在 `_boringssl/orig_backup/`；构建树与脚本在 `_boringssl/` |
| ⚠️ 新教训 | ① NDK clang 忽略 `-static-libstdc++`（用 `-lc++_static -lc++abi` 或 shim）；② BoringSSL CMake 全局 `-fvisibility=hidden` 会让版本脚本导出全失效（sed build.ninja 改 default）；③ 链接静态库必须 `--whole-archive`；④ 版本脚本符号必须按 ABI 从 marsstn UND 逐一生成 |
